# CLAUDE.md

Este archivo es la **única fuente de contexto persistente** para cualquier sesión de Claude Code que se abra en este repo. La mayoría de las sesiones son locales y residuales (corren en compus de la escuela, que están freezadas — no queda historial ni memoria de una sesión a la otra). Esto significa que **este documento tiene que alcanzar por sí solo** para que una sesión nueva entienda el proyecto completo, sepa qué está hecho, qué falta, y cómo trabajar conmigo. No asumas que existe contexto previo fuera de lo que está escrito acá y en `.claude/documents/`.

---

## Cómo trabajar conmigo (instrucciones para la IA)

- **Hablame siempre en español.** El código y los comentarios en inglés están bien, pero la conversación conmigo es en español.
- **Explicá el por qué**, no solo el qué. Si tomás una decisión técnica no obvia, decí brevemente el motivo (no hace falta un ensayo, una línea alcanza).
- **Preguntame antes de actuar, no asumas, en estos casos puntuales:**
  - Cualquier decisión de producto o UX, aunque parezca chica (cambia cómo se ve o comporta la app para el usuario final)
  - Cuando hay ambigüedad técnica — más de una forma razonable de resolver algo y no es obvio cuál preferís
  - **Nunca asumas que algo de una sesión anterior ya se aplicó.** Las sesiones no comparten memoria entre sí — verificá siempre en el código actual antes de decir "esto ya está hecho" o de construir sobre un supuesto de un sync/handoff previo
- Fuera de esos casos, con ambigüedad baja o tareas mecánicas, podés avanzar sin preguntar.
- Soy estudiante (no tengo background profesional de ingeniería de software) — priorizá explicaciones claras sobre jerga, pero no me subestimes: entiendo código y decisiones técnicas si me las explicás.

---

## Cómo funciona el proyecto ahora (modelo de sesiones)

Hasta el 2026-07-01 OPA se desarrollaba con 4 chats especializados en la nube (Frontend+Backend, Design, Database, Documentation) que se coordinaban mediante `.claude/documents/` y un skill `/sync`. **Ese modelo quedó descontinuado** (ver `.claude/documents/_archive/meta-2026-06-07-chat-structure.md` para referencia histórica).

Modelo actual:
- **Sesiones locales**: cada apertura de Claude Code en la compu de la escuela es una sesión de un solo uso, sin separación de roles — la sesión hace de frontend, backend, y lo que haga falta dentro de este repo. Al cerrar, ese contexto se pierde.
- **Un chat en la nube** (este) se mantiene activo para pulir y coordinar cosas más grandes o que requieran contexto acumulado.
- Por eso: **todo cambio relevante tiene que quedar escrito en el repo** (código + `.claude/documents/` + este archivo) antes de terminar una sesión. Si no está escrito, no existe para la próxima sesión.

---

## ¿Qué es OPA?

App mobile de descubrimiento de moda centrada en **outfits** como unidad principal de contenido (TikTok/Pinterest para moda). Tres pilares: descubrimiento de outfits, armario personal, compra contextual.

La infraestructura de backend (Supabase + Edge Functions + Hono API) vive en el repo separado `opa-organization/opa-backend` — ya no está en este repo. Ver "Mapa de repos" abajo.

### Qué NO es OPA (valores y decisiones descartadas)

- **No es un catálogo de e-commerce.** El outfit (contenido) va primero, la compra es contextual y viene después. Nunca se prioriza layout tipo tienda sobre layout tipo feed.
- **No es un feed genérico de moda.** Los pilares son descubrimiento + armario personal + compra — no se agregan features que no sirvan a alguno de los tres.
- **Las marcas no son "vendedores" pasivos** — son creadores de contenido primero (publican outfits armados con sus propias prendas). El catálogo y la venta son secundarios a eso. (Ver `.claude/documents/product-2026-06-10-brand-system.md`.)
- **Decisión descartada:** asignar talles de vendedor por defecto sin guía — se optó por el sistema de `size_guides` + `user_measurements` + recomendación automática en su lugar.
- **Decisión descartada:** un solo modo de venta uniforme — se optó por modelo híbrido (`direct` vs `redirect`) porque no todas las marcas quieren procesar pagos dentro de OPA.

---

## Mapa de repos

OPA vive en 4 repos separados. Si una tarea necesita tocar otro repo, decilo explícitamente — esta sesión probablemente no tiene ese repo clonado ni en su scope de acceso.

| Repo | Stack | Rol | Estado |
|---|---|---|---|
| `opa-mobile` (este repo) | React Native + Expo SDK 54 | App para usuarios finales (consumidores) | Activo, es el más avanzado |
| `opa-backend` (`opa-organization/opa-backend`) | Supabase + Edge Functions + Hono API | Infraestructura compartida entre opa-mobile y opa-web | ✅ Confirmado independiente (2026-07-03): se redeployó la Edge Function `api` en producción usando solo el código de `opa-backend` y se verificó `opa-mobile` funcionando end-to-end contra Supabase sin `backend/` local |
| `opa-admin` (`opa-organization/opa-admin`) | Next.js 14 + Tailwind (sin shadcn CLI instalado, pese al stack declarado) | Panel interno del equipo OPA (moderación, aprobar marcas, KPIs) — NO es para marcas ni usuarios finales | **Funcional localmente contra Supabase real** (confirmado 2026-08-03 leyendo el repo clonado en `C:\Users\devandroid\opa-admin`): login, auth gate por `is_admin`, dashboard, gestión de usuarios y marcas, moderación de outfits/prendas/reseñas. Sin deploy en Vercel. Falta: pantalla de solicitudes de marca (`brand_applications` sin UI), estadísticas por marca/ventas, paginación |
| `opa-web` | Next.js (planeado) | Panel de gestión para marcas (analytics, stock, pedidos) — para uso desde desktop | No iniciado — crear directo bajo `opa-organization` |

Todos comparten el mismo proyecto Supabase (`vecnktrbjolahcalkbml`) y el mismo schema en español.

### Organización de GitHub (2026-07-03)

Los 4 repos de OPA se agrupan bajo la organización **`opa-organization`** en GitHub (antes estaban sueltos bajo la cuenta personal `MaxiBernardoni`).

- ✅ `opa-mobile` y `opa-backend` — transferidos y confirmados (2026-07-03, re-verificado 2026-08-03): `git remote -v` en ambos apunta a `github.com/opa-organization/...`, y funcionan (push/fetch probados).
- ✅ `opa-admin` — transferido a `opa-organization/opa-admin`, confirmado por el usuario (2026-08-03). No se re-verificó con `git remote -v` en esta sesión (el repo no está clonado acá); el usuario lo va a clonar para una próxima sesión, ahí conviene revisar también el estado real del código (si ya tiene el scaffolding de Next.js o sigue vacío).
- `opa-web` todavía no existe — es un pendiente a futuro, deliberadamente pospuesto (2026-08-03). Cuando se cree, crearlo directamente bajo `opa-organization`, no bajo la cuenta personal.

**Nota sobre el método:** para mover un repo ya existente a una org se usa **Settings → Danger Zone → Transfer** (traspaso nativo de GitHub, sin clonar, sin pedir tokens) — no la opción "Import a repository" (esa clona por HTTPS y falla con `Invalid username or token` porque GitHub no soporta auth por password en operaciones git).

---

## Comandos de desarrollo

```bash
# Mobile / Web
npx expo start --web --clear   # Web en browser — funciona en cualquier red
npx expo start --clear          # Expo Go en celular — misma WiFi, SDK 54

# IMPORTANTE: siempre usar --legacy-peer-deps en npm install
npm install --legacy-peer-deps

# API (Deno / Supabase Edge Functions) — repo separado, ver opa-backend
# cd ../opa-backend
supabase functions serve api    # Dev local
supabase functions deploy api   # Deploy a producción
```

---

## Stack técnico

| Capa | Tecnología |
|---|---|
| Framework | React Native + Expo SDK 54 (managed workflow) |
| Navegación | Expo Router v6 (file-based routing) |
| Estado global | Zustand (`store/useAuthStore.ts`) |
| Imágenes | Expo Image |
| Backend | Supabase (Project ID: `vecnktrbjolahcalkbml`) |
| API server | Hono v4 en Deno (Supabase Edge Functions) — código en `opa-backend/functions/api/` (repo separado) |
| Lenguaje | TypeScript (mobile) / TypeScript en Deno (API) |

---

## Design tokens

```ts
// colors.ts
rosaOpa: '#EB006B'         // primario, acciones
negro: '#000000'
blanco: '#FFFFFF'
grisClaro: '#838383'       // texto secundario
grisBorde: '#F2F2F2'       // bordes
grisMedio: '#D9D9D9'       // placeholders
grisOscuro: '#4E4E4E'      // texto terciario
bordeTag: '#A6A6AC'

// fonts.ts — mergeOne (títulos), palanquinDark (botones, usernames)
// radius.ts — card:15, chip:10, button:8, tag:8, avatar:9999
// spacing.ts — xs:4, sm:8, md:12, lg:16, xl:24, xxl:32
```

Assets en Supabase Storage: `https://vecnktrbjolahcalkbml.supabase.co/storage/v1/object/public/assets/`
Los nombres de archivo son **case-sensitive**: `Grid.png` (G mayúscula), iconos de nav en subcarpeta `nav/`.

---

## Esquema de base de datos

Tablas y columnas en **español**:

```
perfiles        — extiende auth.users; trigger handle_new_user() crea fila al registrarse
marcas          — brands; profile_id FK → perfiles
prendas         — garments; brand_id FK → marcas; sale_mode ('direct'|'redirect'), external_url, size_guide_id
outfits         — creator_id FK → perfiles; likes_count/saves_count mantenidos por triggers
outfit_items    — outfit_id + garment_id + slot ('torso'|'piernas'|'calzado'|'extras')
outfit_likes    — UNIQUE(user_id, outfit_id); trigger actualiza outfits.likes_count
outfits_guardados — UNIQUE(user_id, outfit_id); trigger actualiza outfits.saves_count
prendas_guardadas — UNIQUE(user_id, garment_id); prendas en favoritos para comprar
follows         — UNIQUE(follower_id, following_id); trigger actualiza followers/following_count
prendas_armario — armario personal del usuario
productos_carrito — fuente para el checkout (POST /api/orders lo consume y vacía)
orders          — status: pending/shipped/delivered
productos_orden — items de una orden
size_guides     — guías de talle; brand_id nullable (NULL = OPA default)
size_guide_entries — entradas por talle con rangos de medidas
user_measurements — medidas del usuario; RLS estricto (solo fila propia)
brand_applications — solicitudes para ser marca
admin_impersonation_log — auditoría de "login como marca" desde opa-admin (admin_profile_id, brand_id, brand_profile_id, created_at); RLS activado sin policies, solo accesible vía service role
preguntas       — Q&A usuario↔marca (2026-09-07); user_id, brand_id, garment_id nullable (null = pregunta general), question, answer/answered_at nullable = sin responder
prenda_imagenes — galería multi-imagen por prenda (2026-09-14, hasta 5); garment_id FK → prendas ON DELETE CASCADE, sort_order (0 = portada, espejada en prendas.image_url)
```

**Función `get_trending_garments(p_brand_id, p_days, p_limit)` (2026-09-07):** RPC security definer que ordena el catálogo de una marca por guardados recientes (`prendas_guardadas`); alimenta "Prendas en tendencia" en la Home de marca. Ver `database-2026-06-06-schema-and-seed.md`.

---

## Arquitectura del código

### Mobile (`app/`)

```
app/
  _layout.tsx         — fonts, auth init, Stack navigator
  (tabs)/
    index.tsx         — Home: rama por is_brand. Usuario normal: carousels de outfits, prendas, marcas (sin cambios). Cuenta de marca: BrandHomeView — panel de gestión (tráfico, preguntas sin responder, sus outfits, prendas en tendencia, opiniones recientes), ver nota abajo
    outfits.tsx       — Outfit Scroll TikTok-style; acepta ?outfitId para deep-link
    profile.tsx       — Perfil: header horizontal, 3 tabs (grid/favoritos/pedidos), sub-tabs
    search.tsx        — búsqueda funcional: texto + tabs + tag filters
    wardrobe.tsx      — armario personal con datos reales
  auth/index.tsx      — Login/Signup con validación por campo en tiempo real
  settings.tsx        — Logout, eliminar cuenta, "Mis medidas", "Registrar Marca" (si !is_brand)
  measurements.tsx    — inputs numéricos (altura/pecho/cintura/cadera/muslo cm), persiste via useUserMeasurements().save()
  user-outfits.tsx    — Scroll de outfits de un usuario; params: userId, startIndex
  saved-outfits.tsx   — Scroll de outfits guardados; param: startIndex
  outfit/[id].tsx     — detalle de outfit con prendas por slot, precio total, CTA
  product/[id].tsx    — detalle de prenda con SizeGuideSheet, selector de talle, CTA
  user/[id].tsx       — perfil de lectura de otro usuario (no marca separada); redirige a /(tabs)/profile si es el propio
  marca/[id].tsx      — perfil público de una marca (banner + avatar-logo, badge verificada, "ya lo tenés", stats Seguidores/Outfits/Prendas, Seguir, tabs Grid/Catálogo). Hook useBrand. Outfits/Seguidores vacíos hasta que las marcas tengan profile_id (onboarding pendiente); catálogo con datos reales
  followers/[id].tsx  — lista de seguidores/seguidos estilo IG/TikTok (tabs, botón Seguir inline por fila, buscador local). Ver nota debajo
  impersonate.tsx     — pantalla receptora del "login como marca" generado desde opa-admin (ver abajo). Solo web
  brand/
    create-garment.tsx — cargar prenda nueva (cuentas de marca); también editor con ?id=<garmentId>
    questions.tsx      — todas las preguntas sin responder de la marca + responder inline (destino del "→" de la Home de marca)
```

**Flujo de auth:** `_layout.tsx` llama `supabase.auth.getSession()` + `onAuthStateChange()` → popula `useAuthStore` (session + profile). `initialized` previene flashes de UI.

**Login como marca sin password (2026-08-03):** desde `opa-admin` (ficha de marca → botón "Iniciar sesión como esta marca"), un admin puede abrir una pestaña ya logueado como el auth user de esa marca, sin conocer su contraseña — pensado para poder reproducir/depurar cualquier problema que reporte una marca. Mecanismo: `opa-admin` llama `supabase.auth.admin.generateLink({ type: 'magiclink', ... })` con la service_role key (server-side), abre el link resultante en una pestaña nueva; Supabase verifica y redirige a `{NEXT_PUBLIC_MOBILE_APP_URL}/impersonate#access_token=...&refresh_token=...`. `app/impersonate.tsx` acá lee esos tokens del hash (no de `detectSessionInUrl`, que sigue en `false`), llama `supabase.auth.setSession()`, y redirige a `/(tabs)/profile` (que ya hace `<Redirect>` a `/marca/[id]` si `is_brand`). Solo pensado para uso interno vía la build web — no tiene sentido en nativo (deep linking sería otro proyecto). Queda auditado en `admin_impersonation_log` (tabla nueva en Supabase, sin policies públicas — solo accesible vía service role) con quién impersonó a qué marca y cuándo. Detalle de testing: si probás pegando manualmente una URL con hash en una pestaña que ya tenía la app abierta (SPA navigation), el efecto de `impersonate.tsx` puede no re-ejecutarse porque el router no remonta el componente solo por un cambio de hash — en uso real esto no pasa porque el magic link siempre abre una pestaña/carga nueva (full page load), que sí remonta.

**Deep-link Home → Outfit Scroll:** Home pasa `outfitId` como param a `/(tabs)/outfits`. El scroll usa `getItemLayout` + `scrollToIndex` / `scrollToOffset`. Índice 0: `scrollToOffset({ offset: 0 })`.

**Toggle like/save:** INSERT optimista en `outfit_likes` / `outfits_guardados`. Si error `23505` (unique constraint) → ya existía, tratar como éxito. DELETE para deshacer.

**Ver perfil de otro usuario:** antes de `app/user/[id].tsx` no existía esto — tocar un creador en cualquier lado de la app saltaba directo a `user-outfits.tsx` (su scroll de outfits), nunca a un perfil. Los 3 puntos de entrada (`outfit/[id].tsx`, `OutfitScrollItem` del scroll principal, `search.tsx`) ahora navegan a `/user/[id]` primero. Quedaron fuera de alcance (ver pending-features): conexiones mutuas ("Seguís a X y N más en común") y campanita de notificaciones en el botón Seguir — ninguna tenía precedente en el código ni columnas de DB para soportarlas.

**Home de marca + Q&A (2026-09-07):** a pedido explícito del usuario ("que no tengan la misma experiencia"), `app/(tabs)/index.tsx` ahora rama por `profile.is_brand` — una cuenta de marca ve `BrandHomeView`, un panel de gestión completamente distinto al feed de descubrimiento (ver el árbol de `app/` arriba y `frontend-2026-06-06-screens-and-components.md` → "Brand Home" para el detalle completo de las 5 secciones). El pedido original se armó primero como mockup en Claude Design para poder iterar el contenido/orden de las secciones antes de tocar código real (ver historial de esta conversación) — la implementación real terminó siendo más chica que el mockup en dos puntos, ambos por decisión explícita al construir, no por limitación técnica: (1) **"Tráfico" solo muestra métricas reales** (Me gusta/Guardados/Seguidores) — Visitas y Clics a tienda, que sí aparecían en el mockup como placeholder, se sacaron porque no hay ninguna tabla de tracking para eso; (2) **no hay botón "+ Nuevo outfit"** — no existe ninguna pantalla de creación de outfit para marcas en toda la app, así que hubiera sido un botón sin acción real (ver pendientes). La feature de Preguntas y Respuestas es enteramente nueva (tabla `preguntas`, no existía ningún Q&A antes de esto) — un usuario pregunta desde `product/[id].tsx` (sobre una prenda) o `marca/[id].tsx` (sobre la marca en general, botón "Preguntar" al lado de "Seguir"), la marca ve las primeras 3 sin responder en su Home con un botón "→" a `app/brand/questions.tsx` (listado completo, responder es inline ahí mismo). Verificado de punta a punta en browser real con una cuenta de prueba descartable (creada y borrada en la misma sesión): preguntar → aparece en la Home de la marca con badge → responder → desaparece de pendientes, fila confirmada en la DB. Migraciones nuevas: `create_preguntas_table`, `create_trending_garments_function`, `restrict_trending_garments_to_authenticated` (esta última corrige que Supabase le da EXECUTE a `anon` automáticamente al crear una función nueva — un solo `REVOKE ALL FROM PUBLIC` no alcanza, ver `database-2026-06-06-schema-and-seed.md`). Aplicadas directo vía Supabase MCP en esta sesión (no tenía `opa-backend` clonado) — no requirió cambios en `opa-backend` porque todo el acceso a `preguntas` va directo a Supabase por RLS, sin pasar por la API Hono (mismo patrón que el toggle de `descontinuada`).

### Hooks (`hooks/`)

| Hook | Retorna |
|---|---|
| `useOutfits(creatorId?)` | outfits del feed o de un usuario |
| `useProfile(userId)` | perfil de un usuario |
| `useSavedOutfits(userId)` | outfits guardados — expone `refetch()` |
| `useSavedGarments(userId)` | prendas guardadas — expone `refetch()` |
| `useWardrobe(userId)` | armario personal |
| `useLike(outfitId, initialCount)` | like toggle con optimistic update |
| `useSave(outfitId, initialCount)` | save toggle con optimistic update |
| `useFollow(targetUserId)` | follow toggle |
| `useSizeGuide(guideId?)` | guide + entries ordenadas por sort_order |
| `useUserMeasurements()` | measurements + `save()` con UPSERT |
| `useRecommendedSize(guideId?)` | llama `supabase.rpc('get_recommended_size')` |
| `useBrandMetrics(brandProfileId?)` | likes/saves (API) + seguidores (Supabase) — "Tráfico de tu cuenta" |
| `useTrendingGarments(brandId?, days?, limit?)` | catálogo de marca ordenado por guardados recientes (RPC `get_trending_garments`) |
| `useBrandQuestions(brandId?, {limit?})` | preguntas sin responder + `totalCount` + `answer(id, texto)` |
| `useAskQuestion()` | `ask(brandId, texto, garmentId?)` — insertar una pregunta |
| `useBrandReviews(garmentIds, limit?)` | últimas reseñas entre varias prendas (Home de marca) |
| `useGarmentQuestions(garmentId?)` | Q&A **público** de una prenda (todas, respondidas o no) — sección "Preguntas y respuestas" en `product/[id].tsx` |
| `useGarmentImages(garmentId?, fallbackUrl?)` | galería de una prenda (`prenda_imagenes`, hasta 5) — usada en `product/[id].tsx` para el carrusel; cae a `fallbackUrl` (`prendas.image_url`) si no hay filas |

> Nota: esta tabla no está completa desde hace varias sesiones (faltan `useBrand`, `useMyBrand`, `useSaveGarment`, `useCart`, `useGarmentReviews`, etc. — ver `backend-2026-06-06-supabase-integration.md` → "Data Hooks" para la lista real y actualizada).

### API (código en `opa-backend/functions/api/`, repo separado)

Hono v4 en Deno. Base path: `/api`. Auth middleware en todas las rutas protegidas — lee `Authorization: Bearer <token>`, valida con `supabase.auth.getUser()`, expone `c.get('user')` y `c.get('supabase')`.

```
GET  /api/health                       — liveness check
GET  /api/brands/me                    — brand del usuario autenticado
PATCH /api/brands/me                   — actualiza brand info
GET  /api/brands/me/metrics            — likes + saves (visit/click pendiente de tablas DB)
GET  /api/brands/me/prendas            — lista prendas de la marca
POST /api/brands/me/prendas            — crea prenda; external_url requerido si sale_mode='redirect'
PATCH /api/brands/me/prendas/:id       — actualiza prenda con verificación de ownership
GET  /api/orders                       — órdenes del usuario
POST /api/orders                       — checkout: valida stock, crea order, decrementa stock, vacía carrito
PATCH /api/orders/:id/status           — brand owner cambia status (pending/shipped/delivered)
```

Rate limiter in-memory en `POST /orders`: ventana 60s, max 20 req/user. Nota: el rate limiter corre *antes* que el auth middleware en el orden de registro, así que `c.get('user')` está vacío en ese punto y el límite nunca se aplica en la práctica — bug preexistente, pendiente de arreglar (ver pending-features).

---

## Decisiones técnicas críticas

- **`--legacy-peer-deps` obligatorio** en todos los `npm install` — conflictos de peer deps de Expo SDK 54.
- **`newArchEnabled: false`** — incompatible con react-native-screens@4.16.0 en RN 0.81.5.
- **`"updates": { "enabled": false }`** en app.json — sin esto Expo Go descarga el bundle remoto en vez del servidor local.
- **`pointerEvents` como style prop** — en `app/(tabs)/outfits.tsx` el SafeAreaView flotante usa `style={{ pointerEvents: 'box-none' }}`. En RN 0.71+ como prop directo está deprecado.
- **`FlatList` horizontal necesita `style={{ flexGrow: 0 }}` dentro de un contenedor `flex:1` (react-native-web)** — sin esto, el `FlatList` se estira para ocupar todo el alto disponible del padre en vez de ajustarse al contenido (el síntoma visual: una fila de chips/cards que debería medir ~40px termina ocupando toda la pantalla). Ya resuelto una vez en `app/(tabs)/outfits.tsx` (`style={{ height: pageH, flexGrow: 0 }}`, ver nota de 2026-07-13 en Estado actual) y de nuevo en `app/(tabs)/search.tsx` (2026-08-14, chips de filtro de tags/categoría — se había verificado ese cambio solo por DOM/texto, sin captura visual real, y el bug pasó desapercibido hasta que el usuario mandó un screenshot de su celular). **Antes de dar por buena una lista horizontal de chips nueva, revisar que tenga este fix** — es el mismo patrón cada vez que aparece. **Variante 2 del mismo bug, opuesta (colapso, no estiramiento) (2026-09-04):** con `flexGrow:0` puesto, si esa misma fila de chips es hermana de un `FlatList`/grid de resultados que sí necesita crecer y scrollear (mismo contenedor `flex:1`), react-native-web puede *encoger* la fila de chips (default `flex-shrink:1` en todo `View`) para cederle espacio al hermano grande apenas el contenido total no entra en pantalla — la encoge hasta el mínimo de una sola línea de texto (~16px), recortando el chip. Con fondo blanco no se nota; con un chip de fondo sólido (ej. activo/seleccionado) tapa el texto por completo. Poner solo `height` fijo NO alcanza (solo fija el `flex-basis` inicial, no evita el shrink) — hace falta también `flexShrink: 0`. Resuelto en `search.tsx` (`tagListWrapper: { flexGrow: 0, flexShrink: 0, height: 44 }`, ver Estado actual 2026-09-04).
- **Supabase Auth storage:** `expo-secure-store` en nativo, `localStorage` en web. Configurado en `lib/supabase.ts`.
- **Fuente Merge One:** `assets/fonts/MergeOne-Regular.ttf` cargada con `expo-font`. No está en `@expo-google-fonts`.
- **Marco mobile en web (`components/layout/MobileFrame.tsx`):** en web, cuando la ventana es más ancha que un teléfono (`APP_MAX_WIDTH = 393`, iPhone 16 Pro) la app se encuadra en una columna centrada de 393px de ancho × 100% de alto sobre fondo gris (se lee como un celular). En nativo, o cuando la ventana ya es angosta (Chrome responsive / celular real), el marco no se aplica y la app ocupa todo el ancho → responsive automático. El wrapper está en el root (`app/_layout.tsx`) envolviendo el `<Stack>`. **Detalle no obvio:** el alto del marco se setea numérico (= `useWindowDimensions().height`) porque react-native-web no estira confiable con `flex:1` / `'100vh'` cuando el root no tiene alto definido (con `flex:1` la columna colapsaba al alto del contenido).
- **`constants/layout.ts` (`APP_WIDTH`):** todos los cálculos de layout basados en ancho (carruseles, grillas, paginado, ancho de cards) usan `APP_WIDTH` en vez de `Dimensions.get('window').width`. `APP_WIDTH` = ancho real en nativo, capeado a `APP_MAX_WIDTH` en desktop-web, para que el layout interno se calcule contra el ancho del "teléfono" y no del monitor. La **altura** sigue usando `Dimensions.get('window').height` directo (el marco ocupa 100% del alto → la altura de la ventana es la altura visible real; paginado del outfit scroll intacto). Son constantes a nivel de módulo: no se recalculan en un resize en vivo (comportamiento preexistente), pero sí en cada carga.

### Gotchas de proceso (no solo técnicos)

- **No confíes en un checklist `[x]` sin verificar el código.** Ya pasó que un sync entre chats marcó pantallas como "implementadas y mergeadas" cuando en realidad eran stubs — el commit real llegó después. Antes de decir "esto ya está hecho", leé el archivo.
- **`.claude/documents/` puede desactualizarse.** Es la mejor fuente disponible, pero no es infalible — si algo no cuadra con lo que ves en el código, el código manda y hay que corregir el doc.
- **Commits/PRs:** en la práctica, las últimas sesiones (incluida esta) commitean y pushean directo a `main` — el historial real (`git log`) no muestra ramas de feature ni PRs para los cambios recientes, a pesar de que una versión anterior de esta nota decía "rama `claude/trusting-fermi-jfr1ix`, PR draft, squash merge". Si vas a pushear directo a `main`, confirmá con el usuario primero (el harness de Claude Code bloquea el push directo a main sin autorización explícita en el mismo turno, incluso si ya se aprobó "commit y push" en general) — no asumas que un "sí, commit y push" previo ya cubre el push a main.
- **Antes de armar una vista nueva, revisar si el ícono/imagen ya existe en Supabase Storage (`assets/`) en vez de usar texto/emoji como placeholder.** Pasó con `app/user/[id].tsx`: se armó la flecha de back y el ícono de compartir como texto (`←`, `↑`) cuando ya existían `flecha.png` y `compartir.png` en el bucket, sin uso en ningún otro lado del código todavía (hubo que corregirlo en una segunda vuelta a pedido del usuario). Para chequear rápido si un asset existe: `fetch(BASE + 'nombre.png', {method:'HEAD'})` desde el preview del browser (HEAD 200 = existe) — Bash/PowerShell dentro de esta sesión pueden no tener red hacia Supabase por igual (Bash falló con curl, PowerShell con `Invoke-WebRequest` sí funcionó, y también sirvió para descargar la imagen a un archivo temporal y abrirla con Read para ver visualmente el ícono antes de usarlo — por ejemplo para confirmar que `flecha.png` apunta a la izquierda). Si no hay asset para un ícono puntual (se buscó y no existe, ej. no hay ningún ícono de menú "···" en el bucket), ahí sí un texto/emoji como placeholder está bien.
- **Cuidado al probar botones que escriben en la DB (like, save, follow) contra Supabase real: el browser del preview puede tener una sesión de auth persistida de una prueba anterior sin que se note.** Pasó en esta sesión: se buscó confirmar con `Object.keys(localStorage)` que no había sesión antes de un test de follow, pero la sesión real estaba guardada bajo otra clave (no en `localStorage` visible en ese momento) y el click sí ejecutó un `INSERT` real en `follows` en producción — quedó `mateo.h` siguiendo a `vale.rios` (fila `id: c7c9cf7b-6621-45d3-8dc9-7cca7c585f88`, creada 2026-07-03). Se le preguntó al usuario si revertirlo y decidió dejarlo así, así que **ese follow es real, no es basura de seed data** — no lo borres por las dudas sin preguntar primero. Para la próxima: antes de tocar botones de escritura contra el proyecto Supabase real, asumí que puede haber una sesión activa aunque no la hayas iniciado vos en esa conversación, y avisá al usuario en vez de "confirmar" con un chequeo que puede dar falso negativo.
- **El `.env` local (con `EXPO_PUBLIC_SUPABASE_URL` y `EXPO_PUBLIC_SUPABASE_ANON_KEY`) está gitignoreado y NO viaja entre dispositivos ni queda en el repo.** Si en una sesión nueva `npx expo start --web` tira `Uncaught Error: supabaseKey is required`, hay que recrear `.env` a mano. La URL es `https://vecnktrbjolahcalkbml.supabase.co`; la anon key se puede pedir con la tool de Supabase MCP `get_publishable_keys` (`project_id: vecnktrbjolahcalkbml`) — devuelve tanto la legacy anon key (JWT) como la nueva `sb_publishable_...`, cualquiera de las dos sirve como `EXPO_PUBLIC_SUPABASE_ANON_KEY`. También hay que correr `npm install --legacy-peer-deps` si `node_modules` no está (pasó en esta sesión, el entorno era nuevo).
- **`.claude/launch.json` (server para el preview del browser en Claude Code): tenía que setear `BROWSER=none` antes de `npx expo start --web` (2026-09-14).** Sin esto, el proceso arrancaba, compilaba el bundle, y a los ~20-24s se caía solo con `Error: spawn UNKNOWN` (stack apunta a `better-opn`/`open` en `openBrowserAsync`) — Expo CLI, con `--web`, intenta auto-abrir un browser real al terminar de levantar, y ese `spawn` no tiene con qué correr en el entorno headless del preview de esta sesión, así que tira abajo todo el proceso de Metro (no solo falla en silencio). `better-opn` respeta la env var `BROWSER`; seteándola a `none` se saltea el intento de apertura. El fix quedó en el propio `runtimeArgs` del config (no en el código del proyecto): `"runtimeExecutable": "cmd", "runtimeArgs": ["/c", "set BROWSER=none&& npx expo start --web --clear --port 8090"]` en vez de invocar `npx expo start ...` directo. Si en el futuro se corre `npx expo start --web` a mano desde una terminal normal (no desde el preview de Claude Code), este problema no debería aparecer — ahí sí hay un browser real para abrir.

---

## Glosario de dominio

| Término | Significado |
|---|---|
| **outfit** | Unidad de contenido principal — un "look" armado con varias prendas, publicado con imagen de portada |
| **prenda** | Garment individual (remera, pantalón, zapatilla, etc.), pertenece a una marca |
| **slot** | Categoría de posición de una prenda dentro de un outfit: `torso`, `piernas`, `calzado`, `extras` |
| **marca** | Brand — cuenta de Supabase Auth separada de las personales, `perfiles.is_brand = true` |
| **armario** | Wardrobe personal del usuario — prendas que ya tiene, guardadas en `prendas_armario` |
| **guardar / guardado** | Save — bookmarking de un outfit o prenda (no es "like", es para volver a ver/comprar) |
| **talle recomendado** | Size calculado por `get_recommended_size()` en base a `user_measurements` del usuario y el `size_guide` de la prenda |
| **sale_mode** | Modo de venta de una prenda: `direct` (checkout dentro de OPA) o `redirect` (external_url a la tienda de la marca) |

---

## Documentación interna

Los documentos de referencia viven en `.claude/documents/`:

| Archivo | Contenido |
|---|---|
| `frontend-2026-06-06-screens-and-components.md` | Pantallas, componentes, design tokens |
| `backend-2026-06-06-supabase-integration.md` | Auth flow, hooks, tipos TypeScript |
| `backend-2026-06-15-api-layer.md` | Endpoints de la API Hono, estado de implementación |
| `database-2026-06-06-schema-and-seed.md` | Schema completo, migraciones, seed data |
| `design-2026-06-06-visual-system.md` | Sistema visual |
| `product-2026-06-10-brand-system.md` | Modelo de marcas: onboarding, verificación, monetización |
| `meta-2026-06-10-pending-features.md` | **Fuente de verdad de pendientes** — actualizar cuando se implementa algo |
| `_archive/` | Documentos obsoletos del modelo multi-chat anterior — solo referencia histórica, no seguir sus instrucciones |

**Regla:** cuando se completa un pendiente, eliminarlo de `pending-features.md` y marcarlo como hecho en el documento de la capa correspondiente. Al terminar una sesión, si hiciste un cambio relevante, actualizá el doc correspondiente y este `CLAUDE.md` antes de irte — la próxima sesión no va a tener otra forma de saberlo.

---

## Estado actual

**Resumen en prosa (2026-07-03):** la app mobile tiene el flujo principal completo y funcional contra Supabase real — auth, home, outfit scroll estilo TikTok, perfil, búsqueda, armario, detalle de outfit y de prenda con guía de talles, y "Mis medidas". Settings tiene logout, borrado de cuenta, acceso a medidas y el formulario de "Registrar Marca" (que solo inserta una solicitud en `brand_applications`, todavía no crea la cuenta de marca — eso lo hace `opa-admin` a futuro). La API en Hono está desplegada y sin 501s pendientes. **La separación de `opa-backend` ya se completó**: se verificó que el código de `opa-backend` es autosuficiente (redeploy de la Edge Function `api` en producción desde ese repo, health check OK, app mobile probada end-to-end contra Supabase sin `backend/` local) y la carpeta `backend/` se eliminó de este repo el 2026-07-03. De paso se encontró y arregló un bug en el rate-limiter (`app.use('/orders/POST', ...)` no matchea ningún path real, se corrigió a un middleware `'*'` que chequea método+path) y se reconstruyó una migración RLS (`rls_policies_cart_orders_reviews_wardrobe`) que estaba aplicada en la DB pero faltaba en el historial de git de ambos repos — ambos fixes están commiteados en `opa-backend`. Lo que falta es sobre todo features de producto avanzadas (ver Pendientes) y las sub-pantallas de Settings que no son core. También se agregó `app/user/[id].tsx` (2026-07-03): antes de esto no existía ninguna forma de ver el perfil de otro usuario en la app — tocar un creador en cualquier lugar saltaba directo a su scroll de outfits. Se construyó reusando hooks ya existentes (`useProfile`, `useOutfits`, `useFollow`) y se dejaron fuera de alcance, a pedido explícito, las conexiones mutuas y la campanita de notificaciones del botón Seguir (ninguna tenía precedente en el código).

- [x] Setup + design tokens + BottomNavBar custom
- [x] Home screen con carousels horizontales
- [x] Outfit Scroll TikTok-style (like/save/follow funcionales)
- [x] Profile screen (header horizontal, 3 tabs, sub-tabs favoritos)
- [x] user-outfits.tsx y saved-outfits.tsx
- [x] Auth screen con validación por campo en tiempo real
- [x] Settings screen (logout, eliminar cuenta)
- [x] Deep-link Home → Outfit Scroll con scroll al índice correcto
- [x] Supabase Auth funcional (signup, login, logout)
- [x] Hooks: useLike, useSave, useFollow, useSavedOutfits, useSavedGarments
- [x] Sistema de guías de talle (tablas + hooks useSizeGuide, useUserMeasurements, useRecommendedSize)
- [x] API Hono: todos los endpoints implementados (sin 501s pendientes)
- [x] `prendas_guardadas` en DB con RLS
- [x] `app/product/[id].tsx` — detalle de prenda con imagen, selector de talle, SizeGuideSheet, CTA
- [x] `app/outfit/[id].tsx` — detalle de outfit con prendas por slot, precio total, CTA
- [x] `app/(tabs)/search.tsx` — búsqueda funcional: texto + tabs + tag filters
- [x] `app/(tabs)/wardrobe.tsx` — armario con datos reales, filtro por slot
- [x] `app/measurements.tsx` — pantalla "Mis medidas", accesible desde Settings
- [x] Settings → botón "Registrar Marca" + form de solicitud (inserta en `brand_applications`)
- [x] Fuente Merge One en `assets/fonts/MergeOne-Regular.ttf` — cargada en `_layout.tsx`
- [x] Extracción de `backend/` a `opa-organization/opa-backend` — confirmado independiente y `backend/` eliminado de este repo (2026-07-03)
- [x] `app/user/[id].tsx` — perfil de lectura de otro usuario (gap real que no existía: antes solo se podía ver el perfil propio). Reusa `useProfile`, `useOutfits`, `useFollow`. Actualizados 3 puntos de entrada para navegar acá en vez de saltar directo al scroll de outfits (2026-07-03). Pulido en una segunda vuelta el mismo día: flecha de volver (`flecha.png`) y compartir (`compartir.png`) con assets reales de Storage en vez de texto/emoji, ícono de grid centrado en la tab bar, y bottom navbar agregada (esta pantalla está fuera del `Tabs` navigator, así que se armó una versión standalone calcada de `BottomNavBar` con "perfil" marcado como activo)
- [x] `app/marca/[id].tsx` — perfil público de marca "ajeno" (2026-07-06). Construido a spec del doc de brand-system: banner (reusa `logo_url`, no hay columna de cover), avatar-logo circular, nombre en mayúscula + badge `verificado_ondas.png` si `marcas.verified`, `@handle · Marca`, bio, tags, banner contextual "Ya lo tenés" (cruza `useWardrobe` con `garment.brand_id`), stats Seguidores/Outfits/Prendas, botón Seguir, tabs icon-only Grid (outfits) / Catálogo (prendas → `product/[id]`). Hook nuevo `useBrand`. Enganchado desde el slider "Marcas" del home y la fila de marca en el detalle de prenda. Verificado con `tsc` (sin errores nuevos) y compilando el bundle web de Metro (ruta incluida, 200). **Limitación conocida:** todas las `marcas` tienen `profile_id = null` (falta onboarding de cuentas de marca), así que Outfits/Seguidores quedan vacíos y Seguir es inerte para las marcas actuales; el catálogo sí trae datos reales. Se agregó `verified: boolean` al tipo `Brand` en `types/index.ts`

- [x] **Marco mobile + responsive en web (2026-07-06):** en web, si la ventana es más ancha que un teléfono, la app se encuadra en una columna centrada de 393px (iPhone 16 Pro) sobre fondo gris; en móvil / Chrome responsive ocupa todo el ancho. Nuevo `components/layout/MobileFrame.tsx` (wrapper en `app/_layout.tsx`) + `constants/layout.ts` (`APP_WIDTH`/`APP_MAX_WIDTH`). Se migraron todas las lecturas de ancho (`Dimensions.get('window').width`) a `APP_WIDTH` en las ~11 pantallas/componentes que las usaban; la altura sigue leyéndose de `Dimensions` (el marco es 100% de alto). Verificado por inspección de DOM en el preview web (marco 393×800 centrado en desktop; a 375px el marco desaparece y el root ocupa 375px). Ver "Decisiones técnicas críticas".
- [x] **Camión del outfit scroll era emoji 🚚 (2026-07-06):** `app/(tabs)/outfits.tsx` usaba el emoji en el header flotante en vez de la imagen `assets/camion_blanco.png` de Storage (el home ya la usaba bien). Reemplazado por `<Image>` 26×26 — verificado que carga 200 en el preview. Coincide con el spec del design doc ("Do NOT use 🚚 emoji").
- [x] **Outfit scroll alineado al prototipo (2026-07-06):** cuatro correcciones visuales en `app/(tabs)/outfits.tsx` + `components/outfit/OutfitScrollItem.tsx`: (1) **subrayado rosa** de 2px `rosaOpa` bajo la tab activa (implementado como `View` bajo el texto, no border del texto); (2) **pill oscuro removido** de las tabs — ahora texto plano sobre la foto (el usuario eligió match-prototipo por sobre la spec vieja del doc, que pedía `rgba(0,0,0,0.4)`; design doc actualizado); (3) **contadores numéricos eliminados** de like/save (el doc ya decía "NO numeric counters"); (4) **ícono de compartir real** `compartir.png` con `tintColor` blanco en vez de la flecha `↗` de texto; y "Total look" → "Precio total" en la barra inferior. Verificado por DOM en preview web (underline rosa 2px presente, pill ausente, 7 imgs `compartir.png` con tint SVG). Lo que NO se hizo por falta de datos/backend quedó en Pendientes (líneas conectoras, badge de descuento, footer de marca).
- [x] **Barra de precio del outfit scroll se cortaba / quedaba tapada por la tab bar (2026-07-06):** cada item medía `SH = Dimensions.get('window').height` (alto completo de ventana), pero la `BottomNavBar` se dibuja ENCIMA del contenido (no reserva espacio), así que la barra de precio (`bottom:0`) caía detrás de la nav — solo asomaba "PRECIO TOTAL" y el precio + botón "Ver outfit" quedaban tapados (síntoma reportado: "solo se ve al bajar y volver a subir"). **Fix:** en `app/(tabs)/outfits.tsx` se calcula `pageH = SH - tabBarHeight` (con `tabBarHeight = 8 + 48 + (insets.bottom||8) + 1`, replicando el alto real de `BottomNavBar`) y se usa para `snapToInterval`, `getItemLayout` y el alto del item (`OutfitScrollItem` ahora recibe prop `height`). Así cada página mide el área visible sobre la nav y la barra queda pegada arriba de ella. Verificado por DOM (barra blanca 393px de ancho, borde inferior en y=655 = arranque de la nav, botón "Ver outfit" visible sin scrollear). **Nota:** el screenshot del preview timeouteaba con las imágenes full-screen cargadas (la captura, no el render — el `eval` respondía normal), se verificó todo por medición de DOM. De paso se agregó el **ícono de bolsa rosa** (`bag_rosa.png`, ya existía en Storage, lo usa `marca/[id].tsx`) a la izquierda del precio en contenedor `rosaOpaLight`, como pide el design doc.
- [x] **Outfit scroll: quitar banda oscura del fondo + iconos de acción al estilo prototipo (2026-07-13):** dos ajustes de fidelidad en `components/outfit/OutfitScrollItem.tsx`. (1) Se **eliminó el `gradientOverlay`** (un rectángulo de 200px `rgba(0,0,0,0.35)` sobre la foto que, encima de fotos oscuras, se veía como una banda negra sólida en la zona del creador + barra de precio) — ahora la foto es full-bleed y la barra flota limpia sobre ella, como el prototipo. (2) **Botones de acción** (like/save/share): se removió el **círculo blanco de fondo** (y su sombra) — ahora son iconos blancos (`♥/♡ ★/☆` y `compartir.png` con `tintColor` blanco) directamente sobre la foto, con `textShadow`/shadow sutil para legibilidad, más grandes y espaciados (34×34, gap 20). Verificado por DOM en el preview: 0 overlays `rgba(0,0,0,0.35)`, glifos con `color:white` y `parentBg:transparent`. La legibilidad del nombre del creador (texto blanco sin overlay) quedó bien sobre las fotos actuales; si aparece una foto muy clara donde no se lea, evaluar un gradiente real (transparente→oscuro) en vez del rectángulo plano que se quitó.
- [x] **Barra de precio no aparecía al pasar de outfit en web (2026-07-13):** síntoma reportado — al bajar al siguiente outfit no se veía el precio, y para verlo había que bajar y volver a subir. **Causa raíz:** el viewport del `FlatList` en `app/(tabs)/outfits.tsx` medía el alto completo del contenedor (`flex:1` = `SH`) mientras que cada item medía `pageH = SH - tabBarHeight` (más chico, para dejar lugar a la nav). Ese desajuste viewport≠item descoloca el snap. **Detalle clave de web:** en react-native-web 0.21.2 `snapToInterval` es un no-op — el único snap en web lo da `pagingEnabled` (aplica `scroll-snap-type: y mandatory` + `scroll-snap-align: start` por item). Como el viewport era más alto que el item, el snap enganchaba desalineado y la barra (`bottom:12` del item) caía fuera de vista; un re-scroll manual la reacomodaba. **Fix:** se forzó el alto del `FlatList` a `pageH` (`style={{ height: pageH, flexGrow: 0 }}`) para que viewport == item, y se removió el `snapToInterval` redundante (dejando solo `pagingEnabled`, que es lo que funciona en web). Verificado por DOM en el preview a 375×812: el scroller mide `clientH=747=pageH` (`scrollH=5229=7×747`), y en el 2º outfit la barra "Precio total"/"Ver outfit" queda en y≈680–711 dentro del viewport de 747 (por encima de la nav). **Nota:** `SH` sigue siendo un `Dimensions.get('window').height` a nivel de módulo (no se recalcula en un resize en vivo de la ventana web) — no es el bug reportado, pero si en el futuro aparece un desajuste tras redimensionar la ventana, es por acá.
- [x] **Login de vendedor/marca + perfil propio de marca (2026-07-13):** ahora existe una cuenta de marca real que puede iniciar sesión y ver su propio perfil de marca. Se reutilizó la marca existente **Revés** (`220cc733-dd7c-4f4f-912d-72d465e1196e`, 7 prendas reales) en vez de crear una vacía. La cuenta Auth se creó **a mano vía SQL** (INSERT en `auth.users` con `extensions.crypt(...)` para el hash del password, tokens en `''`, email confirmado) porque no hay endpoint de admin/onboarding todavía; el trigger `handle_new_user` creó `perfiles` y después se seteó `perfiles.is_brand=true` + `marcas.profile_id`. **Credenciales de prueba: `reves@opa.com` / `reves1234`** (son seed/demo, no una cuenta personal — el password lo elegí yo para testing). Frontend: hook nuevo `hooks/useMyBrand.ts`; `app/(tabs)/profile.tsx` hace `<Redirect>` a `/marca/[id]` cuando `profile.is_brand`; `app/marca/[id].tsx` ganó un modo `isOwn` (`brand.profile_id === session.user.id`): engranaje de configuración (→ `/settings`, ahí está el logout) en vez de compartir/menú, sin botón "Seguir", y el tab "perfil" de la navbar activo. **Verificado a nivel DB** (hash de password válido — GoTrue valida exactamente así —, rol/confirmado/tokens OK, marca↔perfil↔7 prendas vinculados) y con `tsc`. **NO se verificó el render en browser** esta sesión: el dev server de Expo no se mantenía vivo en este entorno (requests del bundle daban `ERR_CONNECTION_REFUSED`, y hubo fallas de spawn de procesos — PowerShell tiraba `uv_spawn`). Detalle: Revés ahora tiene `profile_id` real, así que su grilla de Outfits / Seguidores / Seguir están vivos pero leen 0 (no tiene outfits ni followers en seed); las **otras** marcas siguen con `profile_id = NULL`. Pendientes derivados (ver pending-features): bloquear like/save/follow para cuentas de marca, y switcher multi-cuenta personal⇄marca.
- [x] **`app/impersonate.tsx` — receptor del login-como-marca sin password (2026-08-03):** nueva feature en `opa-admin` (ver `opa-admin/CLAUDE.md`) que permite a un admin abrir la app logueado como cualquier marca con cuenta vinculada, sin conocer su contraseña, para poder reproducir/depurar problemas que reporte esa marca. Este repo solo aporta la pantalla receptora: lee `access_token`/`refresh_token` del hash de la URL (el link lo genera `opa-admin` vía `supabase.auth.admin.generateLink`), llama `supabase.auth.setSession()`, y redirige a `/(tabs)/profile` (que ya resuelve a `/marca/[id]` si `is_brand`). Probado end-to-end contra Supabase real con la cuenta de **Revés** (en ese momento única marca con `profile_id`; ver bullet siguiente — ya no es la única): el link generado aterrizó correctamente en su perfil propio. Auditado en la tabla `admin_impersonation_log` (Supabase, sin policies públicas). Ver detalle completo en la nota "Login como marca sin password" más arriba (sección Arquitectura del código).
- [x] **Cuentas de login para el resto de las marcas con catálogo cargado (2026-08-03):** de las 7 `marcas` seed, solo Revés tenía cuenta de Auth (ver bullet 2026-07-13 arriba). Se repitió el mismo mecanismo manual (INSERT en `auth.users` con `extensions.crypt(...)`, trigger `handle_new_user` crea `perfiles`, luego `UPDATE perfiles SET is_brand=true` + `marcas.profile_id`) para las 3 marcas que ya tienen prendas reales cargadas: **Capas** (`capas@opa.com` / `capas1234`, 4 prendas), **Forma** (`forma@opa.com` / `forma1234`, 7 prendas), **Sole** (`sole@opa.com` / `sole1234`, 7 prendas) — mismo patrón de credenciales que Revés, son seed/demo. **Batuk, Doble V y Midway quedaron sin cuenta a propósito** (0 prendas cargadas cada una — un perfil propio se vería vacío igual que el catálogo; decisión del usuario, no una limitación técnica). A diferencia de la sesión anterior, esta vez **sí se verificó el render en browser**: se conectó al preview (`preview_start` con `{url: "http://localhost:8090"}`, reusando un dev server que ya estaba corriendo desde otra sesión/chat) y se hizo login real con `capas@opa.com` — redirige a `/marca/[id]` propio (nombre, bio, tags, stats reales, sin botón "Seguir"), y `/settings` muestra la cuenta de Capas correctamente. Se cerró sesión al terminar para no dejar el navegador logueado en un dev server compartido con otra sesión. Con esto, 4 de las 7 marcas (Revés, Capas, Forma, Sole) tienen login funcional; las otras 3 quedan pendientes de si en el futuro cargan prendas.
- [x] **Segunda pasada de fidelidad al prototipo del outfit scroll (2026-07-06):** cinco ajustes más señalados por el usuario sobre una captura. En `app/(tabs)/outfits.tsx`: (1) **separador "/" eliminado** entre "tus marcas" y "Descubrir" (el prototipo no lo tiene); (2) **círculo del botón "+" removido** (queda el signo fino, `fontSize:28 weight:300`, sin borde). En `components/outfit/OutfitScrollItem.tsx`: (3) **líneas conectoras** chip→prenda con forma de codo (segmento horizontal + diagonal, `View`s rotados con `transformOrigin`) terminando en un punto blanco (`connLine`/`connDot`). **Cómo se resolvió sin coords en la DB:** el ancla de cada prenda se deriva de su `slot` (`SLOT_ANCHOR`: extras/torso/piernas/calzado → fracción x/y sobre la figura), los chips se ordenan por slot y se separan verticalmente para no solaparse (anti-overlap con `lastBottom`). No es pixel-perfect por prenda (es aproximación por slot) pero da el look desestructurado del prototipo. Chips angostados a 118px para que la línea tenga largo visible; (4) **like/favorito/compartir** subidos a ~40% del alto y con **fondo blanco circular** 44px (iconos oscuros, share `compartir.png` con `tintColor:negro`); (5) **barra de precio flotante** (`left/right/bottom:12`, `borderRadius` en las 4 esquinas) en vez de pegada al fondo. Verificado por screenshot + medición DOM a 375×812 (líneas 39–87px con codo, 3 círculos apilados a la derecha, barra flotante con gap 12px sobre la nav, "Ver outfit" visible). **Gotcha del preview:** el preview headless renderiza la app a ancho 0 hasta que se fuerza un viewport con `preview_resize` (preset mobile 375×812) — sin eso, `getBoundingClientRect`/`innerWidth` dan 0 y el screenshot timeoutea. Con el viewport seteado, todo funciona.
- [x] **Tab "Armario" reemplazado por "Catálogo" para cuentas de marca (2026-08-07):** a pedido del usuario, una cuenta de marca (`profile.is_brand`) ya no tiene sentido con un armario personal — en su lugar ve su propio inventario. Mismo tab/ruta (`wardrobe`), rama por tipo de cuenta: `components/navigation/BottomNavBar.tsx` muestra `bag_negra.png`/`bag_rosa.png` en vez de `armario.png`/`armario_rosa.png` cuando `useAuthStore().profile.is_brand` es true; `app/(tabs)/wardrobe.tsx` renderiza `BrandCatalogView` en vez de `PersonalWardrobeView`. La nueva vista tiene dos sub-tabs (decisión del usuario: sub-tabs en vez de todo apilado) — **Prendas**: grid con `useMyBrand()` + `useBrand()` (mismos hooks que `marca/[id].tsx`), mostrando **stock total sumado** por prenda (decisión del usuario: total, no desglose por talle) vía el campo `stock_por_talle` (jsonb) que ya existía en la DB pero le faltaba al tipo `Garment` — se agregó (`types/index.ts` + doc de integración). **Outfits**: grid de outfits creados por esa marca (mismo dato que ya mostraba `marca/[id].tsx` en modo propio, ahora también accesible desde este tab). Solo lectura al momento de escribir esto — **nota (2026-08-10): ya no es así, ver el bullet de "Crear prenda" más abajo**, que agregó creación (no edición todavía) directo desde este mismo tab. Verificado con `tsc` (sin errores nuevos) y en el preview web logueado como **Capas** (`capas@opa.com`): navbar muestra `bag_rosa.png` activo / `bag_negra.png` inactivo, tab Prendas muestra 4 prendas con "50 en stock" cada una, tab Outfits muestra el empty state correcto (0 outfits, Capas no tiene ninguno creado). Confirmado que una cuenta normal / sesión anónima sigue viendo el armario de siempre sin cambios. Detalle en `.claude/documents/frontend-2026-06-06-screens-and-components.md` (secciones Wardrobe y BottomNavBar). **Nota (2026-08-10): el bullet de arriba quedó desactualizado el mismo día que se escribió** — el ícono real de catálogo (`catalogo.png`/`catalogo_rosa.png`, un ícono de lista/planilla) recién se subió a Storage después; hasta entonces el tab usaba `bag_negra.png`/`bag_rosa.png` como placeholder. Ver bullet siguiente.
- [x] **Ícono real de "Catálogo" en el navbar de cuentas de marca (2026-08-10):** el usuario subió `catalogo.png`/`catalogo_rosa.png` a la raíz del bucket `assets` (no en `nav/`) y pidió reemplazar el placeholder de bolsa. Encontrados y confirmados visualmente (`Read` sobre los PNG descargados por PowerShell) antes de usarlos — mismo protocolo que otros assets. Cambiado en `components/navigation/BottomNavBar.tsx`. **Bug encontrado en la misma tarea:** el ícono seguía siendo `armario.png` en los dos navbars *standalone* que viven fuera del `Tabs` navigator y por eso no pueden reusar `BottomNavBar` — `app/marca/[id].tsx` (perfil propio de marca) y `app/user/[id].tsx` (perfil de otro usuario visto por una cuenta de marca) tienen su propio array `NAV_TABS` hardcodeado con `armario.png` sin lógica de `is_brand`. Se agregó el mismo criterio (`viewerIsBrand` → `catalogo.png`/`catalogo_rosa.png`) en ambos, más el ícono de la sub-tab "Catálogo" dentro del perfil propio de marca (que además usaba el viejo placeholder `bag_negra.png`/`bag_rosa.png`). Verificado en browser real logueado como Capas: perfil propio y perfil de `@vale.rios` vistos como marca, sin ningún rastro de `armario.png` en ninguno de los dos navbars.
- [x] **`app/product/[id].tsx` — rediseño completo "estilo Shein" (2026-08-10):** a pedido explícito del usuario ("pensemos cómo va a ser la view de una prenda... que tenga todas las funcionalidades que OPA necesita... que esté habilitado abrir una prenda en todo momento"). Antes de esto, varios botones de la vista de prenda no hacían nada — "Agregar al carrito" y "Ver en tienda" no tenían `onPress`, no había botón de guardar, `stock_por_talle` (con datos reales en la DB) no se usaba para nada, y los dos puntos de la app donde más se ven prendas (los chips flotantes del scroll principal estilo TikTok, y el carrusel "Últimas Prendas" de Home) tampoco eran tocables. Antes de programar se confirmaron 4 decisiones de producto con el usuario: (1) **no** armar un bottom-sheet global de "vista rápida" — arreglar los puntos muertos para que naveguen a la pantalla completa existente; (2) "Agregar al carrito" hace un INSERT real en `productos_carrito`, más una pantalla de carrito **básica y temporal** (`app/cart.tsx`, sin checkout); (3) sección de reseñas con estado vacío (la tabla `reseñas` existe pero tiene 0 filas — requiere `order_id`, o sea compra verificada); (4) imagen única con zoom, sin tocar el schema para agregar galería (cada prenda tiene 1 sola `image_url`). Implementado: selector de talle consciente del stock (deshabilita/tacha talles agotados, urgencia "¡Últimas N unidades!"), stepper de cantidad, botón guardar/wishlist real (`prendas_guardadas`, hook nuevo `useSaveGarment`), fila "Más de esta marca", visor de zoom full-screen (`components/product/ZoomableImage.tsx`), redirect real vía `Linking.openURL`. Nuevos hooks `useCart.ts` y `useGarmentReviews.ts`. Se conectaron los dos puntos muertos: `OutfitScrollItem`'s chip flotante de prenda y la card del carrusel "Últimas Prendas" de Home. **Detalle técnico de una vuelta atrás:** la primera versión del zoom usaba `PanResponder` para pellizco+paneo continuo, pero en este entorno (react-native-web sin `GestureHandlerRootView` montado) dejaba el sistema de responders trabado — una vez abierto el visor, ningún botón de la pantalla volvía a responder. Se reemplazó por una versión sin `PanResponder` (solo doble-tap, mismo mecanismo `onPress` que el resto de los botones). Verificado extensamente en browser real logueado como Capas: navegación desde los 3 puntos de entrada, selector de talle, stepper, guardar (INSERT/DELETE real en `prendas_guardadas`), agregar al carrito (INSERT real, badge y toast), pantalla `/cart` completa (stepper, sacar item, total), apertura del visor de zoom. **Limitación de testing, no de producto:** cerrar el visor de zoom vía click simulado no funcionó en el browser headless de esta sesión — se confirmó que es una limitación general de ese entorno con `Modal` de RN (el `SizeGuideSheet` preexistente, no tocado, tiene el mismo problema), no algo roto en el código nuevo; falta una prueba en un dispositivo/browser real. Detalle completo en `.claude/documents/frontend-2026-06-06-screens-and-components.md` y pendientes derivados (checkout real, bug preexistente del botón "Ver outfit") en `meta-2026-06-10-pending-features.md`.
- [x] **`app/brand/create-garment.tsx` — pantalla de crear prenda para cuentas de marca (2026-08-10):** a pedido del usuario ("terminar el crear prenda"), pensando juntos dónde debía vivir. **Decisión importante que revisa un supuesto viejo:** toda la documentación anterior (`product-2026-06-10-brand-system.md`, y varios bullets de este mismo archivo) daba por sentado que la gestión de prendas iba a vivir en `opa-web` (repo de escritorio, sin iniciar) — el usuario pidió construirla en `opa-mobile` en su lugar, en el tab Catálogo → Prendas que la marca ya usa (`app/(tabs)/wardrobe.tsx`), con un botón "+ Agregar prenda". No es una reversión total del plan de `opa-web` — publicar outfits, métricas y gestión de pedidos siguen asumidos para ahí — solo la carga de prendas, porque fue lo que se pidió y encajaba naturalmente donde la marca ya ve su catálogo. Antes de programar se confirmaron 2 decisiones con el usuario: (1) subida real de imagen (no URL pegada a mano) — primera vez que la app sube un archivo desde el cliente, nueva dependencia `expo-image-picker` + plugin en `app.json`; (2) guía de talles custom **originalmente pedida pero recortada a mitad de camino**: al investigar se encontró que `size_guide_entries` (las medidas por talle) solo acepta INSERT de `service_role` vía RLS — no hay forma de que esto funcione sin un endpoint nuevo en `opa-backend`, y esta sesión no tiene ese repo clonado ni `supabase`/`gh` CLI ni el MCP de Supabase autorizado, así que no hay manera de construirlo ni desplegarlo hoy. Se lo planteé al usuario con el hallazgo concreto y decidió dejarlo para una sesión que sí tenga acceso a `opa-backend` — el formulario final solo deja elegir entre guías ya existentes (las 10 de OPA, filtradas por categoría con el mismo mapeo torso→tops/piernas→bottoms que ya usa `app/product/[id].tsx`). **Lo que quedó armado:** imagen (sube a Storage, bucket `assets/prendas/{marca}/...`, extensión derivada del `mimeType` real del archivo — parsear la URI local no sirve en web porque es un `blob:` sin extensión), nombre/descripción/precio, categoría (chips), color/estilo (texto libre), talles+stock dinámicos según categoría (XS–XXL o EU 35–42 para calzado), guía de talles opcional, modo de venta directo/redirect con URL condicional. Nuevo método `api.createGarment()` en `lib/api.ts` — **primera vez que `opa-mobile` llama `POST /api/brands/me/prendas`**, un endpoint que ya existía en `opa-backend` (comentado "para opa-web") pero nunca lo usaba el móvil; funcionó sin cambios del lado del backend. `hooks/useBrand.ts` ganó `refetch()`, invocado con `useFocusEffect` en `BrandCatalogView` para que la prenda nueva aparezca al volver sin lógica extra. **Dos hallazgos reales durante el testing en browser** (logueado como Capas, con creación real verificada de punta a punta contra Supabase): (1) `prendas.image_url` tiene `NOT NULL` en la DB — no estaba documentado como tal, el formulario la trataba como opcional y el primer submit sin imagen falló con el error crudo de Postgres; se corrigió a campo obligatorio. (2) **Una marca no puede borrar sus propias prendas** — probado con un DELETE real contra `prendas` usando la sesión de Capas: devuelve `200` pero no borra nada (RLS sin policy de owner-delete). Esto dejó una prenda de prueba real (`Buzo Test Automatizado`, id `b654f80e-2e9c-423e-a938-f15a967ef2f1`) en el catálogo de Capas — se le avisó al usuario en el momento (no se asumió que no importaba) y decidió borrarla él mismo desde `opa-admin` (moderación, que sí usa `service_role`). Detalle completo, y los 3 pendientes que salen de esto (editar/borrar prenda, guía custom, endpoint de borrado), en `.claude/documents/frontend-2026-06-06-screens-and-components.md`, `product-2026-06-10-brand-system.md` y `meta-2026-06-10-pending-features.md`.

- [x] **`prendas.image_url` documentado como `NOT NULL` + "borrar prenda" resuelto como "descontinuar" (2026-08-14):** dos hallazgos del bullet de `create-garment.tsx` (2026-08-10) cerrados en esta sesión. (1) Solo doc: se anotó explícitamente `image_url` como `NOT NULL` en `database-2026-06-06-schema-and-seed.md` (ya estaba resuelto en código desde el 10, `validate()` en `create-garment.tsx` ya exige imagen). (2) **Borrar prenda:** antes de tocar nada se investigaron las FK reales hacia `prendas` — `outfit_items`/`prendas_armario`/`prendas_guardadas` son `ON DELETE CASCADE` (un DELETE real rompería silenciosamente outfits ajenos) y `productos_orden`/`reseñas` son `ON DELETE NO ACTION` (el DELETE falla directo si hay historial). Se lo planteé al usuario con este hallazgo antes de elegir camino; decidió que las marcas no borren de verdad — en su lugar pueden marcar una prenda como **"Descontinuada"**: nueva columna `prendas.descontinuada` (boolean, default false, migración `add_descontinuada_to_prendas`), toggle en `app/(tabs)/wardrobe.tsx` (`BrandCatalogView` → pill "Descontinuar"/"Reactivar" sobre cada card). Una prenda descontinuada desaparece del catálogo público (`marca/[id].tsx`, `search.tsx`, "más de esta marca" en `product/[id].tsx`) y su compra queda bloqueada ahí (CTA "Ya no disponible" + banner), pero sigue intacta en cualquier outfit que ya la use. Borrado real de verdad sigue existiendo solo como pedido a soporte vía `opa-admin` (`service_role`). **Detalle técnico:** el PATCH de la API (`PATCH /api/brands/me/prendas/:id`) tiene una whitelist de campos que no incluye `descontinuada` — confirmado empíricamente probando el PATCH real desde el browser logueado como Capas antes de asumir nada (con `color` funcionó 200 OK, con `descontinuada` dio 400 "Cannot coerce the result to a single JSON object", indicando que el UPDATE quedaba con un SET vacío). Como esta sesión no tiene `opa-backend` clonado para arreglar la whitelist, se agregó en su lugar una policy RLS `brand_owner_update_own_prendas` (UPDATE, owner-scoped) y el toggle escribe directo a Supabase — trade-off documentado: esa policy es más permisiva de lo ideal (deja a la marca tocar cualquier columna de sus propias prendas sin las validaciones de negocio de la API). Verificado en Supabase real (columna creada, policy aplicada, PATCH de prueba revertido sin dejar residuo). Detalle completo en `database-2026-06-06-schema-and-seed.md`, `product-2026-06-10-brand-system.md`, `frontend-2026-06-06-screens-and-components.md` y `meta-2026-06-10-pending-features.md`.

- [x] **CORS bloqueaba crear prenda desde un `expo start --web` sin puerto fijo (2026-08-14):** el usuario reportó `Access to fetch ... blocked by CORS policy` al crear una prenda desde `localhost:8081` (el puerto por defecto de Expo cuando no se pasa `--port`). La allowlist del Edge Function `api` solo tenía `localhost:8090` hardcodeado (el puerto pineado de esta sesión). En vez de agregar otro puerto fijo (mismo problema la próxima vez que cambie), se cambió `origin` a una función que acepta cualquier `http://localhost:<puerto>` en dev + los orígenes de prod explícitos. **Deployeado directo vía el MCP de Supabase** (`deploy_edge_function`, versión 12) — esta sesión no tiene `opa-backend` clonado, así que el fix está en producción pero el repo de `opa-backend` quedó desactualizado (mismo tipo de drift que ya pasó una vez con una migración, ver `database-2026-06-06-schema-and-seed.md`). Verificado con un preflight OPTIONS real (`Origin: http://localhost:8081` → `204` + `Access-Control-Allow-Origin` correcto). **Pendiente real:** sincronizar el código nuevo al repo de `opa-backend` en una sesión que lo tenga clonado. Detalle en `backend-2026-06-15-api-layer.md`.

- [x] **Cerrar sesión en una pestaña cerraba la sesión en todas (2026-08-14):** reportado por el usuario probando dos cuentas en simultáneo. Causa raíz: no es un bug propio del código de esta app — `supabase-js` sincroniza sesión entre pestañas del mismo origen (web) vía un `BroadcastChannel` único por proyecto (no por cuenta): cualquier `signOut()` en una pestaña difunde `SIGNED_OUT` a todas las demás pestañas abiertas, sin importar qué cuenta tenían activa, porque el canal se llama igual (`sb-<project>-auth-token`) sin importar el usuario. Esta app permite explícitamente cuentas distintas en pestañas separadas (es la base del switcher multi-cuenta), así que ese comportamiento por default rompía justo el caso de uso que la app quiere soportar. **Fix** en `lib/supabase.ts`: apenas se crea el cliente, se cierra (`.close()`) y se limpia la referencia (`broadcastChannel = null`) al canal interno del SDK — deliberadamente accede a un campo `protected` (no público) del SDK vía cast a `any`, documentado en el propio archivo con el riesgo de que una futura actualización de `@supabase/supabase-js` renombre ese campo y el fix deje de aplicarse en silencio. De paso se sacó código muerto en `app/(tabs)/profile.tsx`: había un segundo `handleLogout()` (con `signOut()` sin `scope:'local'`, inconsistente con el de `settings.tsx`) que no estaba conectado a ningún botón. **Verificado con dos pestañas reales** logueadas con cuentas distintas (Capas y Sole): cerrar sesión en una ya no afecta a la otra mientras ambas sigan abiertas sin recargar. **Límite que sigue existiendo, no es lo que se reportó ni se tocó:** las dos pestañas comparten la MISMA clave de `localStorage` para la sesión persistida — si una pestaña se recarga (F5) después de que la otra inició sesión con otra cuenta, la que se recarga va a leer lo último que haya en esa clave (puede terminar mostrando la cuenta equivocada, o deslogueada, según el orden). El switcher multi-cuenta (`/switch-account`) sigue siendo el único camino confiable para tener más de una cuenta activa en este dispositivo — usar dos pestañas del navegador en simultáneo es principalmente un artefacto de testing/desarrollo, no el flujo pensado para usuarios reales (que usan la app nativa, sin pestañas).
- [x] **El bullet de arriba no era la causa real que el usuario reportaba (2026-08-14, misma sesión):** después del fix de `BroadcastChannel`, el usuario confirmó "sigue ocurriendo lo mismo" — pero probando otra vez surgió que su flujo real era de una sola pestaña, secuencial, con el switcher: inició con una cuenta personal (Mateo Herrera), después "Cambiar de cuenta → Agregar cuenta" con una cuenta de marca (Forma), y al cerrar sesión de Forma no volvía a quedar disponible Mateo — las dos quedaban inaccesibles. Reproducir ese flujo exacto con una cuenta de prueba descartable + Forma **no** reprodujo el bug (la cuenta personal seguía disponible y el switch de vuelta funcionaba) — la diferencia real resultó ser el tiempo entre pasos: mi prueba scripteada es instantánea, un usuario real tarda más. Revisando `lib/rememberedAccounts.ts` con eso en mente apareció una **race condition real**: `upsertRememberedAccount`/`removeRememberedAccount` hacen "leer todo → modificar → escribir todo" sin ningún tipo de bloqueo — si dos llamadas se disparan casi al mismo tiempo (ej. un `TOKEN_REFRESHED` en segundo plano de la cuenta que ya estaba activa, justo cuando se dispara el `SIGNED_IN` de la cuenta nueva), la segunda escritura pisa a la primera con datos viejos. Como Supabase rota los refresh tokens en cada uso, el token de la primera cuenta quedaba guardado desactualizado → la próxima vez que se intentaba volver a ella desde el switcher, ese token ya no servía ("sesión vencida") y se sacaba de la lista también — coincide exactamente con "se me cierran ambas". **Verificado el mecanismo con una simulación aislada** (mismo patrón de lectura/escritura con delays artificiales, fuera del código de la app): sin cola, la escritura concurrente efectivamente pierde el token actualizado; con cola, no. **Fix** en `lib/rememberedAccounts.ts`: todas las operaciones de lectura/escritura (`upsert`, `remove`, y ahora también `getRememberedAccounts`) se encadenan sobre una cola en memoria (`writeQueue`), así cada una parte del estado ya actualizado por la anterior en vez de una foto vieja. `tsc` limpio y flujo normal (login → agregar cuenta → cerrar sesión → volver) reverificado sin regresión en el browser real. **Nota honesta:** no se pudo reproducir la race exacta contra la cuenta real de Mateo Herrera (no tengo su contraseña) — el diagnóstico se arma a partir de: los datos de `auth.sessions` de esa cuenta (3 sesiones creadas en ~30 min, consistente con reintentos), el mecanismo probado matemáticamente, y que es la única causa encontrada que explica el síntoma exacto reportado. Si el usuario prueba de nuevo y sigue pasando, hay que seguir investigando desde acá.
- [x] **Bug del switcher — causa raíz real encontrada y arreglada (2026-08-14, sesión posterior, con los logs `[DEBUG]` que había dejado la sesión anterior):** los logs de diagnóstico (pegados por el usuario) mostraron algo que contradice el diagnóstico previo — `localStorage['opa_remembered_accounts']` en realidad **nunca perdía ninguna cuenta**: en la secuencia completa (Mateo → agregar Forma → cerrar sesión de Forma) el storage terminaba correctamente con `['mateo@opa.com']`. El bug no era de datos — era de **routing**: el gate de "sin sesión" en `app/(tabs)/profile.tsx` (`if (!session) return <pantalla con solo "Iniciar sesión"/"Crear cuenta">`) no tiene ningún camino hacia `/switch-account`, y `/switch-account` en sí solo es alcanzable desde Settings, que a su vez solo es alcanzable con `session` activa. Resultado: apenas te quedás sin sesión (por cualquier logout), quedás atrapado en el login gate aunque el dispositivo tenga otras cuentas recordadas — se siente como "se cerraron las dos cuentas" aunque los datos de la otra cuenta siguen intactos. **Confirmado el síntoma exacto reportado por el usuario** ("no aparece el switcher, me aparece como si no tuviese ninguna cuenta") antes de tocar código. **Fix** en `app/settings.tsx`: `handleLogout()` y `handleDeleteAccount()` ahora hacen un snapshot de `getRememberedAccounts()` **antes** de llamar `signOut()`/borrar la cuenta (para no depender de la carrera contra el `removeRememberedAccount()` que dispara en paralelo el listener de `_layout.tsx`), y el `router.replace()` final va a `/switch-account` si queda alguna otra cuenta, o a `/(tabs)` si no queda ninguna. **Verificado end-to-end en browser real** (login Capas → agregar Sole vía switcher → cerrar sesión de Sole → aterriza en `/switch-account` con Capas disponible → tocar Capas reingresa sin pedir contraseña → cerrar sesión de Capas, ya sin otras cuentas → aterriza en Home normal, no en un switcher vacío). Los tres fixes de la sesión anterior (`BroadcastChannel` entre pestañas, cola en `rememberedAccounts.ts`, guard de `useFollow`) siguen siendo válidos como mejoras — ninguno era la causa de este síntoma puntual, pero tampoco están de más. Logs `[DEBUG auth]`/`[DEBUG remembered]` ya sacados de `app/_layout.tsx` y `lib/rememberedAccounts.ts`.
- [x] **`/switch-account` sin flecha de volver cuando no hay sesión (2026-08-14, mismo día):** a pedido del usuario — si caíste ahí sin sesión activa (el caso del fix de arriba), no tiene sentido ofrecer "volver atrás" porque no hay ninguna pantalla autenticada a la que volver; tenés que elegir una cuenta sí o sí. `app/switch-account.tsx` ahora oculta la flecha `←` y cambia el subtítulo a "Elegí una cuenta para continuar" cuando `session` es `null` (llegada por auto-redirect); si hay `session` activa (llegada normal desde Settings → "Cambiar de cuenta"), la flecha sigue igual que antes. Verificado en browser real ambos casos.
- [x] **Home distinta para cuentas de marca + Q&A entre usuario y marca (2026-09-07):** a pedido explícito del usuario ("quiero que la home de la marca y del usuario sean distintas... que no tengan la misma experiencia"). Primero se armó como mockup estático en Claude Design (canvas de diseño integrado a Claude Code) para poder iterar el contenido antes de tocar código — el usuario pidió 2 ajustes sobre ese mockup (Preguntas: solo las primeras 3 + "ver más" a una vista aparte con todas; Opiniones: siempre las últimas 3, nunca "ver más") y aprobó el resto, y después pidió explícitamente construirlo en el proyecto real. Antes de programar se confirmaron 3 decisiones de alcance con el usuario (las 3 recomendadas): (1) "Preguntas sin responder" es una feature completa de punta a punta (tabla nueva + flujo de preguntar/responder), no solo la pantalla; (2) "Prendas en tendencia" se define como más guardada en los últimos 7 días (`prendas_guardadas`), no simplemente lo más nuevo; (3) "Tráfico" muestra solo métricas que ya existen de verdad (Me gusta/Guardados/Seguidores) — nada de inventar Visitas/Clics en la app real aunque el mockup los mostraba. `app/(tabs)/index.tsx` ahora rama por `profile.is_brand`: `ConsumerHomeView` (el Home de siempre, sin ningún cambio) vs. `BrandHomeView` nueva, con las 5 secciones pedidas. Nuevas piezas: tabla `preguntas` (RLS: quien pregunta ve/crea las suyas, el dueño de la marca ve/responde las suyas), función SQL `get_trending_garments` (security definer, ver nota de permisos abajo), hooks `useBrandMetrics`/`useTrendingGarments`/`useBrandQuestions`/`useAskQuestion`/`useBrandReviews`, pantalla nueva `app/brand/questions.tsx` (listado completo + responder inline), y el punto de entrada para preguntar en `app/product/[id].tsx` y `app/marca/[id].tsx` (botón "Preguntar" al lado de "Seguir"). **Dos recortes de alcance decididos sin preguntar, bajo un criterio ya establecido en este mismo archivo (no dejar botones sin acción real):** no se construyó el botón "+ Nuevo outfit" del mockup (no existe ninguna pantalla de creación de outfit para marcas en toda la app — sería el gap más grande que queda en el sistema de marcas, ver pendientes) y el usuario que pregunta todavía no tiene ninguna forma de ver la respuesta (ni pantalla ni notificación — la RLS ya lo permitiría, falta la UI). **Hallazgo de permisos real durante la implementación:** Supabase le da `EXECUTE` a `anon`/`authenticated`/`service_role` automáticamente al crear una función nueva en `public` (grant directo a cada rol, no vía `PUBLIC`) — un primer `REVOKE ALL ... FROM PUBLIC` sobre `get_trending_garments` no le sacó el acceso a `anon` como se esperaba; hizo falta un `REVOKE EXECUTE ... FROM anon` explícito (migración `restrict_trending_garments_to_authenticated`), confirmado antes y después contra `information_schema.routine_privileges`. Todo aplicado directo vía Supabase MCP (sin `opa-backend` clonado esta sesión) — no hizo falta tocar `opa-backend` porque `preguntas` va todo directo a Supabase por RLS, sin pasar por la API Hono (mismo patrón que el toggle de `descontinuada`). **Verificado de punta a punta en browser real** logueado como `capas@opa.com` (Tráfico 1/2/1 reales, Prendas en tendencia con las 4 prendas reales de Capas todas en "sin guardados esta semana", estados vacíos correctos en Preguntas/Outfits/Opiniones) y con una cuenta de prueba descartable creada solo para este test (preguntar sobre una prenda real de Capas → aparece en su Home con badge y horario → responder desde `app/brand/questions.tsx` → desaparece de pendientes, fila confirmada en la DB con `answer`/`answered_at`; también probado el botón "Preguntar" de pregunta general en `marca/[id].tsx`) — cuenta y preguntas de prueba borradas al terminar. Detalle completo en `.claude/documents/frontend-2026-06-06-screens-and-components.md` ("Brand Home", "Brand Questions"), `database-2026-06-06-schema-and-seed.md` (tabla `preguntas`, función `get_trending_garments`), `product-2026-06-10-brand-system.md` y `meta-2026-06-10-pending-features.md`.

- [x] **Barra de precio del outfit scroll se descolocaba con un resize en vivo de la ventana web (2026-09-07):** reportado por el usuario probando en Chrome DevTools → modo Responsive, cambiando dimensiones sin recargar. **Causa raíz:** `app/(tabs)/outfits.tsx`, `components/outfit/OutfitScrollItem.tsx`, `app/user-outfits.tsx` y `app/saved-outfits.tsx` leían `Dimensions.get('window').height` **a nivel de módulo** (una sola vez, al cargar el módulo) — ya estaba anotado como pendiente latente en una nota de 2026-07-13 ("si en el futuro aparece un desajuste tras redimensionar la ventana, es por acá"). Un resize posterior a la carga (DevTools, o en mobile real la barra de direcciones del navegador apareciendo/desapareciendo) dejaba `pageH` desactualizado, descolocando el `FlatList`. **Fix:** los 4 archivos pasaron a leer el alto con el hook `useWindowDimensions()` (reactivo) en vez de `Dimensions.get('window')` (estático) — mismo patrón que ya usa `components/layout/MobileFrame.tsx`. Se evaluó también la alternativa de hardcodear un set de resoluciones "estándar del mercado" (propuesta inicial del usuario) y se descartó: no ataca la causa (el problema es la falta de recálculo, no un tamaño puntual sin cubrir) y fragmenta mal en Android, que tiene muchísima variedad de pantallas. `constants/layout.ts` (`APP_WIDTH`/`APP_HEIGHT`) **no se tocó** — sigue con el mismo patrón estático, pero no era el bug reportado (es ancho, no alto, y once solo se usa en otras pantallas sin este síntoma); queda como mismo pendiente latente si alguna vez se necesita. **Limitación de verificación honesta:** el `resize_window` del browser headless de esta sesión no dispara el evento DOM `resize` que React Native Web escucha (confirmado leyendo `node_modules/react-native-web/dist/exports/Dimensions/index.js`), así que no se pudo reproducir visualmente el resize-en-vivo en el preview — se verificó por código (mismo patrón que `MobileFrame.tsx`, ya probado en producción) y por carga fresca en distintos tamaños (funciona bien). Falta una confirmación del usuario en su propio Chrome real.
- [x] **Lista de seguidores/seguidos estilo Instagram/TikTok (2026-09-07):** antes de esto, los números "Seguidores"/"Seguidos" ya se mostraban en 3 lugares (`app/(tabs)/profile.tsx`, `app/user/[id].tsx`, `app/marca/[id].tsx`) pero eran texto plano, no tocable — no existía ninguna pantalla que listara quién sigue a quién. Decisiones de UX confirmadas con el usuario antes de programar: botón Seguir/Siguiendo inline en cada fila (no hace falta entrar al perfil), buscador que filtra la lista ya cargada (client-side, sin query nueva), y el perfil de marca solo muestra "Seguidores" sin tabs (hoy ninguna marca sigue a nadie, no hay UI para eso). **Implementado:** hook `hooks/useFollowList.ts` (2 queries — `follows` filtrado por `follower_id`/`following_id` según el tipo, después `perfiles.in(ids)` — mismo patrón que `useFollowedBrandIds.ts`; si un resultado es `is_brand`, un tercer paso resuelve `marcas.id` por `profile_id` para poder navegar a `/marca/[id]` en vez de `/user/[id]`), componente `components/profile/FollowListRow.tsx` (avatar+nombre, botón Seguir vía `useFollow` por fila con `stopPropagation` para no disparar la navegación al tocarlo, oculto si el viewer es una cuenta de marca o es la fila del propio usuario), y pantalla nueva `app/followers/[id].tsx` (standalone, sin bottom navbar — como `user-outfits.tsx`/`saved-outfits.tsx` — con tabs Seguidores/Siguiendo condicionales a `!profile.is_brand`). Los 3 puntos de stats ahora son `TouchableOpacity` que navegan a `/followers/${id}?type=followers|following` (para `marca/[id].tsx`, solo si `brand.profile_id` existe — si la marca no tiene onboarding, el stat queda sin tocar). **Verificado extensamente en browser real** con datos reales (no seed inventado): logueado como Capas (marca, `capas@opa.com`) → "Seguidores" (1 real) muestra a `mateo.h` sin botón Seguir (viewer es marca) → tocar la fila navega a `/user/[id]` → su stat "Seguidos" (3) muestra tabs, lista con `capas.oficial`/`reves.oficial`/`vale.rios`, tocar una marca de la lista navega a `/marca/[id]` (confirma la resolución `brandId`) — buscador probado con match y sin-match. Con una cuenta de prueba descartable (viewer no-marca): botón Seguir inline hace INSERT/DELETE real en `follows` (contador se actualiza, `stopPropagation` confirmado — tocar el botón no navega), verificado en ambas direcciones (seguir y dejar de seguir sin salir de la lista). Cuenta de prueba borrada al terminar (`DELETE FROM auth.users` vía MCP de Supabase — el botón "Eliminar cuenta" de la propia app falló, ver pendiente nuevo abajo; verificado 0 filas en `auth.users`/`perfiles`/`follows` después). `tsc` limpio (los errores de `constants/mockData.ts` son preexistentes, archivo sin uso real). **Fuera de alcance, no pedido:** el link "Seguidores/Seguidos" ya no aparece en el componente huérfano `components/profile/ProfileStats.tsx` porque ese archivo no se usa en ningún lado del código (verificado con grep) — las 3 pantallas reales tienen su propio array de stats inline, y son esas las que se tocaron.

- [x] **`MobileFrame` mostraba el marco gris en teléfonos anchos (2026-09-07):** reportado por el usuario emulando un iPhone 16 Pro Max (440px de ancho) en Chrome DevTools — el marco gris de "columna tipo teléfono" aparecía aunque estuviera simulando un celular real, no una ventana de desktop. **Causa raíz:** el criterio (`components/layout/MobileFrame.tsx`) era solo `width > APP_MAX_WIDTH` (393px, ancho del iPhone 16 Pro usado como referencia) — cualquier teléfono real más ancho que esa referencia (Pro Max ronda 430-440px, muchos Android 412-480px) quedaba mal clasificado como "ventana de desktop". No era solo un artefacto de DevTools: un usuario real con uno de esos teléfonos abriendo la versión mobile-web de OPA vería el mismo margen gris no deseado. **Fix:** se suma un segundo criterio — `(pointer: coarse)` (CSS media feature que distingue touch de mouse; Chrome también la emula al elegir un dispositivo en DevTools) — así el marco solo aparece con `width > 393` **y** puntero fino (mouse, desktop real). Nuevo hook interno `useIsCoarsePointer()` en el mismo archivo, reactivo vía `matchMedia(...).addEventListener('change', ...)`. Verificado en el preview: a 440×956 con touch emulado, el marco ya no aparece (`root` ocupa los 440px completos); a 1200×800 sin touch, el marco sigue apareciendo igual que antes (393px, sin regresión). `tsc` limpio.
- [x] **Franja vacía al costado del contenido en teléfonos anchos, causada por el fix anterior (2026-09-07, misma sesión):** el fix de `MobileFrame` de arriba dejó de mostrar el marco gris en 440px de ancho con touch — pero `constants/layout.ts` (`APP_WIDTH`, usado para carruseles/grillas/cards en más de 10 pantallas) seguía capando el ancho del contenido a `Math.min(win.width, 393)` **sin mirar el touch**, por el criterio viejo (`Platform.OS === 'web' ? min(width, 393) : width`). Resultado: el contenedor medía 440px (ya sin marco) pero el contenido adentro se calculaba contra 393px, dejando una franja vacía/negra al costado — visto por el usuario en `/outfits` y `/profile`. **Fix:** `APP_WIDTH` ahora usa exactamente el mismo criterio que `MobileFrame` (`Platform.OS === 'web' && !isCoarsePointer` para capear) en vez de solo `Platform.OS === 'web'` — los dos lugares que deciden "¿esto es desktop o teléfono?" ya no pueden divergir. Sigue siendo un valor estático de módulo (no reactivo a un resize en vivo), mismo alcance/limitación que ya tenía `APP_WIDTH` antes — no se convirtió a hook porque tocaría los 10+ archivos que lo importan como constante, fuera de alcance de este fix puntual. Verificado en el preview: a 440×956 con touch, la imagen full-bleed del outfit mide los 440px completos (antes 393px con franja); a 1200×800 sin touch, el marco sigue en 393px sin regresión. `tsc` limpio.
- [x] **`APP_WIDTH` convertida de constante estática a hook reactivo `useAppWidth()` (2026-09-07, misma sesión, refactor grande a pedido explícito del usuario):** el fix anterior dejó expuesto que `APP_WIDTH` nunca se recalculaba en absoluto — ni siquiera en un resize en vivo real (es un `const` de módulo, se evalúa una sola vez cuando el JS carga, para siempre, en cualquier browser, no es una limitación de este entorno de testing). Reportado por el usuario cambiando de "iPhone 16 Pro Max" a "iPhone XR" en el dropdown de DevTools **sin recargar la página**: las cards del grid quedaban calculadas para el ancho viejo (136px, pensado para 440px) dentro de un contenedor que ya medía 414px — se veían grandes y la última se cortaba. Reproducido y confirmado en el preview antes de tocar nada (carga en 440 → resize en vivo a 414 sin `navigate` → cards seguían en 136px con el borde derecho en 428px, por fuera del contenedor de 414px). Se le explicó al usuario que el disparador real (cambiar de tamaño sin recargar) es sobre todo un patrón de testing en DevTools — en producción real pasaría solo si el usuario rota la pantalla de su celular o redimensiona la ventana de desktop sin recargar — y se le preguntó si valía la pena el refactor grande (toca 10 archivos) o alcanzaba con documentarlo como "recargá después de cambiar de dispositivo en DevTools"; eligió el refactor completo. **Cambios:** `hooks/useIsCoarsePointer.ts` nuevo (extraído de `MobileFrame.tsx`, que ahora lo importa en vez de duplicarlo); `constants/layout.ts` reemplaza `export const APP_WIDTH` por `export function useAppWidth()` (mismo criterio `Platform.OS==='web' && !isCoarsePointer` que ya usa `MobileFrame`, ahora reactivo vía `useWindowDimensions()`); `APP_HEIGHT` se eliminó del archivo (no tenía ningún consumidor real, confirmado por grep). Los 10 archivos que importaban `APP_WIDTH` como constante (`profile.tsx`, `wardrobe.tsx`, `index.tsx`, `search.tsx`, `outfit/[id].tsx`, `user/[id].tsx`, `product/[id].tsx`, `marca/[id].tsx`, `OutfitScrollItem.tsx`, y el componente huérfano sin uso real `OutfitGrid.tsx`) pasaron a llamar `useAppWidth()` dentro del componente y mover los cálculos derivados (anchos de card, `sidePad` del carrusel de home, alto de imagen de portada) de nivel de módulo a dentro del render, aplicados inline en el `style` en vez de en el `StyleSheet.create` estático (que no puede leer un valor reactivo). **Verificado en el preview:** con carga fresca en 414px, las cards ahora miden 127px sin desbordar (antes de este fix, en el mismo escenario de resize-sin-reload, medían 136px con el borde en 428px); en desktop 1200px sin touch, el marco y las cards (393px / 120px) quedan idénticos a antes del refactor, sin regresión. **Limitación de verificación honesta, ya documentada antes en este archivo para el fix de altura:** el `resize_window` de este entorno de testing no dispara el evento `resize` del DOM (confirmado de nuevo, incluso con un `dispatchEvent` manual) así que no se pudo demostrar visualmente la reactividad en vivo en este preview — se verificó por carga fresca en cada ancho (da el resultado correcto) y por lectura de código (mismo hook ya usado y probado en `MobileFrame`). `tsc` limpio.

- [x] **Una marca no puede comprar ni guardar en su propio catálogo + editor de prenda (2026-09-07, sesión posterior):** a pedido explícito del usuario ("cuando abro una prenda no debería tener ni la opción de agregar al carrito ni de añadir a favoritos... debería tener la opción de editar en vez de eso"). Antes de tocar nada se confirmó en vivo contra la API real (PATCH de no-op sobre una prenda de Capas, revertido) que `PATCH /api/brands/me/prendas/:id` acepta los mismos 12 campos que el POST de creación — evitó construir el editor sobre un supuesto sin probar. `app/product/[id].tsx` ahora resuelve `isOwnGarment = viewerIsBrand && myBrand?.id === garment.brand_id` (nuevo `useMyBrand` en el archivo) y rama en 3 casos: consumidor normal sin cambios; marca en su propia prenda → sin botón de guardar (★/☆) ni carrito en el header, CTA de abajo pasa a "Editar prenda"; marca viendo la prenda de **otra** marca → sin guardar/carrito y sin ningún CTA (ni comprar ni editar algo ajeno tiene sentido). El editor reusa `app/brand/create-garment.tsx` en vez de una pantalla nueva — acepta `?id=<garmentId>`, precarga los 10 campos reales desde `prendas` (RLS pública) y llama el `PATCH` en vez del `POST`; si la prenda no es de la marca logueada (alguien fuerza la URL) muestra un error en vez del form, aunque la escritura ya estaba protegida por RLS/API de todos modos. **Detalle no obvio:** el `useEffect` que resetea talles/guía al cambiar de categoría (para cuando el usuario elige otra a mano) se hubiera disparado también al precargar la categoría por primera vez, borrando el stock recién cargado — se resolvió con un `useRef` que salta ese reset una única vez, justo después de la precarga. Cierra un pendiente viejo ("Garment edit screen — no way to update...") que llevaba abierto desde el 2026-08-10. **Verificado de punta a punta en browser real:** logueado como `capas@opa.com`, prenda propia sin ★/carrito y con "Editar prenda"; formulario de edición precargado con los 10 campos reales de "Trench Camel"; cambio de precio guardado con un PATCH real (confirmado en la DB, revertido después); prenda de Forma vista como Capas sin ningún CTA al final. `tsc` limpio. Detalle completo en `.claude/documents/frontend-2026-06-06-screens-and-components.md` ("Product Detail", "Create Garment") y `product-2026-06-10-brand-system.md`.
- [x] **Q&A reworkeado a público, estilo Mercado Libre + se sacó "Preguntar" del perfil de marca (2026-09-07, sesión posterior):** a pedido explícito del usuario ("quiero que lo hagamos estilo mercado libre... puedas leer preguntas que hicieron otros usuarios... al lado del seguir... sacalo no quiero que esté"). Dos cambios: (1) **`app/marca/[id].tsx` vuelve a tener un solo botón "Seguir"** full-width — se sacó por completo el botón "Preguntar" que se había agregado unas horas antes en la sesión de la Home de marca (imports, estado, handler y estilos de `useAskQuestion`/`askOpen`/`questionText` limpiados del archivo, no dejados como código muerto). (2) **`app/product/[id].tsx`: la sección de preguntas pasó de privada (solo el asker y la marca la veían) a pública** — nueva sección "PREGUNTAS Y RESPUESTAS" con el form para preguntar arriba (sin cambios funcionales) y debajo la lista COMPLETA de preguntas de esa prenda (respondidas o no), visible para cualquiera que entre a la publicación, logueado o no — no solo las propias. Requirió una policy RLS nueva (`public_select_garment_questions`, `garment_id IS NOT NULL`, migración del mismo nombre) ya que las policies existentes solo dejaban ver la propia pregunta o las de la marca dueña. Nuevo hook `hooks/useGarmentQuestions.ts`. La marca sigue respondiendo solo desde su Home + `app/brand/questions.tsx` — no se agregó un control de responder en la página pública, para no duplicar esa UI. Esto además cierra, sin que hiciera falta ninguna pantalla nueva, el pendiente de la sesión anterior ("el usuario que pregunta no ve la respuesta en ningún lado"): ahora la ve ahí mismo, en la publicación. **Verificado de punta a punta en browser real** con una cuenta de prueba descartable (creada y borrada en la misma sesión): pregunta creada aparece al instante en la lista (marcada "Todavía sin responder"), sigue visible tras deslogearse (`localStorage.clear()` + reload, confirmando que la policy pública funciona y no es solo la sesión propia leyendo su propia fila), Capas la responde desde `app/brand/questions.tsx`, la respuesta aparece pública también deslogueado. Perfil de marca de Capas confirmado sin ningún rastro de "Preguntar". `tsc` limpio. Detalle completo en `.claude/documents/frontend-2026-06-06-screens-and-components.md` ("Product Detail", "Brand Profile"), `database-2026-06-06-schema-and-seed.md` (tabla `preguntas`) y `product-2026-06-10-brand-system.md`.
- [x] **Chip "Todo" + panel de filtros avanzados plegable en el buscador general, tab Prendas (2026-09-14):** a pedido explícito del usuario. Antes de programar se confirmaron 2 decisiones de UX (ambas con la opción recomendada): (1) precio/orden, que ya existían como fila siempre visible, se **mueven adentro** del panel plegable en vez de quedar aparte; (2) Color/Estilo dentro del panel son **multi-select** (varios chips a la vez, OR dentro del mismo campo). El chip **"Todo"** va primero en la fila de categorías (`CATEGORY_TAGS`, antes de Torso/Piernas/Calzado/Extras) y muestra las 4 categorías mezcladas al tocarlo — no viene pre-seleccionado por default (hay que tocarlo, igual que el resto de los chips). El ícono plegable usa los assets que subió el usuario a la raíz del bucket (`filtro_negro.png` cerrado / `filtro_rosa.png` abierto), confirmados por HEAD request antes de usarlos. **Color y Estilo eran texto libre en la DB en el momento de este cambio** (`prendas.color`/`prendas.style`, no un enum — el color se estandarizó unas horas después, ver bullet siguiente; estilo sigue siendo texto libre), con datos reales inconsistentes en mayúsculas — se aplicó el mismo criterio que ya usan los chips de Outfits (`outfitTags`): traer los valores reales existentes en vez de inventar una lista, deduplicados ignorando mayúsculas. El filtro de color/estilo se aplica **client-side** (no con `.ilike()/.or()` de PostgREST) porque la tabla de prendas es chica — evita armar/escapar un string OR a mano; el `.limit()` de la query subió de 30 a 100 para no truncar antes de ese filtro. Verificado en browser real (desktop y 375px mobile): "Todo" mezcla categorías, el ícono cambia según `filtersOpen`, Color multi-select (Negro+Gris) trae la unión, "Limpiar" resetea sin tocar la categoría activa, chips de Color/Estilo wrappean bien en mobile sin romper el layout. `tsc` sin errores nuevos. Detalle completo en `.claude/documents/frontend-2026-06-06-screens-and-components.md` ("Search").
- [x] **`prendas.color` estandarizado — de texto libre a paleta fija de 17 + `CHECK constraint` (2026-09-14, misma sesión, un rato después):** a pedido explícito del usuario, que encontró en producción colores como `"cognac"` y `"Negro con mucho texto también"` (ver bullet anterior, mismo día) y pidió cortarlo de raíz: "que no puedas poner [texto libre]... que hayan colores estandarizados... entrá a la base de datos y cambialo". Se armó una paleta candidata en 2 rondas — la primera propuesta (16 colores, con Cognac/Camel/Crema/Azul Marino propios) el usuario la corrigió: "el cognac no y camel tampoco... fijate los que usa mercado libre y grandes empresas para que no haya ni más ni menos" — no se pudo entrar a mercadolibre.com.ar (firewall de red de esta sesión bloquea la categoría "Shopping"), así que la paleta final se armó con la taxonomía de color estándar de e-commerce de moda (Amazon Moda, ML y similares suelen compartir el mismo set), explicitando esa limitación al usuario antes de que aprobara. **Paleta final (17):** Negro, Blanco, Gris, Beige, Marrón, Rojo, Naranja, Amarillo, Verde, Azul, Celeste, Violeta, Rosa, Bordo, Dorado, Plateado, Multicolor — vive en `constants/garmentColors.ts` (`GARMENT_COLORS`, mobile) y en el `CHECK constraint` de la DB, **sin tabla de lookup, dos listas hardcodeadas que hay que mantener en sync a mano**. Migración `standardize_prendas_color_palette` (vía Supabase MCP, `opa-backend` no clonado esta sesión): (1) UPDATE de normalización sobre las 28 prendas reales existentes — consolidación semántica en 5 casos (`camel/crema/off-white→Beige`, `cognac/marrón oscuro→Marrón`, `navy→Azul`, `negro washed`/`"Negro con mucho texto también"→Negro`), resto solo normalización de mayúsculas; (2) `ALTER TABLE prendas ADD CONSTRAINT prendas_color_check CHECK (color IS NULL OR color IN (...))` — el constraint es la parte que realmente cierra el problema: sin él, cualquier camino de escritura (la API, o la policy RLS `brand_owner_update_own_prendas`, ya documentada como permisiva — deja escribir cualquier columna directo) podía volver a meter texto libre sin que la UI se enterara. `app/brand/create-garment.tsx`: el campo Color pasó de `TextInput` a chips de selección única (mismo patrón que Categoría/Talles, toggle para deseleccionar ya que sigue siendo opcional). `app/(tabs)/search.tsx` en un primer momento no se tocó — su filtro de color seguía trayendo valores reales de la tabla, así que pasó de mostrar 13 valores sucios a mostrar solo los 6 colores estandarizados en uso hoy (Azul, Beige, Blanco, Gris, Marrón, Negro). **Esto último resultó ser un error de criterio, corregido en el bullet siguiente el mismo día.** **Verificado con un PATCH real contra la DB** (Capas, "Trench Camel": Beige→Marrón, confirmado en la DB, revertido a Beige después para no dejar el dato de prueba cambiado) — pasa el `CHECK constraint` sin error; 28/28 prendas reales confirmadas dentro de la paleta tras la migración (conteo por color sumado y verificado). `tsc` limpio. Detalle completo en `database-2026-06-06-schema-and-seed.md` (columna `color`) y `frontend-2026-06-06-screens-and-components.md` ("Create Garment").
- [x] **Filtro de color del buscador: mostrar siempre las 17 opciones de la paleta, no solo las que ya tienen prenda (2026-09-14, corrección sobre el bullet anterior, mismo día):** el usuario corrigió el criterio de arriba — quería "colores básicos como rosa, rojo, verde, etc, aunque no haya ninguna prenda con este color, que aparezcan como opción". `app/(tabs)/search.tsx` cambió el filtro de Color del panel plegable (ver bullet de "Chip Todo + panel de filtros" más arriba) de "valores reales de la tabla" (mismo patrón que Estilo/`outfitTags`) a la lista fija `GARMENT_COLORS` importada de `constants/garmentColors.ts` — el mismo archivo que ya usa `create-garment.tsx`, así que ahora hay una sola fuente para "qué colores existen en la app" del lado del cliente (la DB sigue siendo la fuente de la verdad real, vía el `CHECK constraint`). El fetch dinámico de colores (`loadGarmentFilterOptions`) se sacó del todo — Estilo sigue trayendo valores reales sin cambios, solo Color pasó a la lista fija. Verificado en browser: el panel muestra los 17 colores completos (antes solo 6); tocar "Rosa" (ninguna prenda tiene ese color hoy) activa el chip y muestra "Sin resultados" en vez de no poder elegirlo. `tsc` limpio. **Nota: la lista bajó a 16 y el mecanismo de "Otro" cambió la parte de la DB — ver el bullet siguiente, mismo día.** Detalle en `frontend-2026-06-06-screens-and-components.md` ("Search").
- [x] **Color/Estilo pasan a selector desplegable con swatch + opción "Otro" (2026-09-14, tercera vuelta sobre este tema, mismo día):** el usuario pidió tres ajustes más sobre lo de arriba: (1) sacar "Multicolor" de la paleta (quedan 16); (2) agregar **"Otro" con texto libre** — un color legítimo que no está en la lista tiene que poder cargarse igual, no solo los 16 estándar; (3) que **Color y Estilo sean "desplegables"** (no chips sueltos) y que cada color tenga un **circulito con el color al lado del nombre**. Se construyó `DropdownField` (componente nuevo en `app/brand/create-garment.tsx`): un campo tipo "select" que abre una hoja (`Modal`, mismo patrón que `SizeGuideSheet` de `app/product/[id].tsx`) con la lista + fila final "+ Otro" que revela un `TextInput` inline (con "‹ Volver a la lista" y "Listo"). Color usa `GARMENT_COLORS` (ahora con `hex` por color, `constants/garmentColors.ts`) + `GARMENT_COLOR_HEX` para pintar el swatch (16px, círculo, borde gris) en cada fila y en el campo colapsado; un color que vino de "Otro" se muestra con swatch gris genérico (no se conoce su hex). Estilo usa el mismo componente sin `swatches`, con las opciones dinámicas de siempre + "Otro" también (forzar un dropdown 100% cerrado sobre un campo sin enum real bloquearía introducir un estilo nuevo). **Consecuencia de DB, no pedida explícitamente pero necesaria:** un "Otro" con texto libre es incompatible con el `CHECK constraint` de lista cerrada del bullet anterior — se reemplazó (`ALTER TABLE prendas DROP CONSTRAINT prendas_color_check`) por uno liviano (`prendas_color_length_check`: no vacío, ≤30 caracteres, migración `relax_prendas_color_check_allow_custom`) que ya no exige pertenencia a la paleta. Esto es un trade-off consciente, comunicado pero no re-preguntado (la instrucción del usuario de agregar "Otro" ya lo implicaba mecánicamente) — la defensa "ningún texto libre puede colarse nunca" del bullet anterior queda descartada a propósito. De paso: se extrajo `dedupeCaseInsensitive` (antes duplicada en `search.tsx`) a un helper compartido `lib/text.ts`, usado ahora también por el fetch de opciones de Estilo en `create-garment.tsx`. El filtro de color del buscador (bullet anterior) también ganó el circulito de swatch al lado de cada chip, y bajó de 17 a 16 opciones (sin Multicolor) — no necesitó una opción "Otro" porque filtra sobre datos que ya existen, no crea contenido nuevo. **Un detalle de infraestructura de esta sesión, no del código de la app:** el servidor del preview de Claude Code quedó con caché de Metro corrupta a mitad de esta tanda de ediciones (`TransformError: Duplicate declaration "dedupeCaseInsensitive"` con contenido de un archivo que ya no coincidía con el disco) — se resolvió reiniciando el proceso del preview (`expo start --web --clear` ya limpia la caché al arrancar), no fue un bug real del código. Verificado en browser real logueado como Capas: hoja de Color con 16 swatches + "+ Otro"; escribir "Turquesa" vía Otro guarda con un PATCH real (confirmado en la DB); reabrir el editor con ese valor custom reabre en modo "Otro" con el texto precargado; volver a la lista y elegir "Beige" revierte correctamente (confirmado en la DB). `tsc` limpio. Detalle completo en `database-2026-06-06-schema-and-seed.md` (columna `color`) y `frontend-2026-06-06-screens-and-components.md` ("Create Garment", "Search").
- [x] **Color/Estilo colapsados por default dentro del panel de filtros del buscador (2026-09-14, cuarta vuelta sobre este tema, mismo día):** el usuario no quería ver los 16 colores + todos los estilos apenas abría "Filtros". `app/(tabs)/search.tsx`: cada sección (Color, Estilo) ahora tiene su propio header tocable con chevron (`colorSectionOpen`/`styleSectionOpen`, `false` por default) — expandir una no afecta a la otra. El label muestra un contador cuando hay filtros activos y la sección está colapsada (ej. "COLOR (1)"), para que el usuario vea que hay un filtro aplicado sin reabrir la lista. Precio y Ordenar por siguen siempre visibles (no se pidió colapsarlos). Verificado en browser real (desktop y 375px): panel abre con ambas secciones colapsadas; expandir Color muestra los 16 swatches sin afectar Estilo; elegir "Rojo" y volver a colapsar muestra "COLOR (1)" con el filtro seguir aplicado. `tsc` limpio. Detalle en `frontend-2026-06-06-screens-and-components.md` ("Search").
- [x] **Centro de notificaciones completo (2026-09-14):** a pedido explícito del usuario — campanita en el Home (arriba a la derecha, con `campana_negra.png`/`campana_rosa.png` que subió), notificaciones para ambos tipos de cuenta (usuario: te siguieron/te dieron like/te guardaron un outfit; marca: te hicieron una pregunta) y para el usuario que pregunta (le respondieron), y una pantalla con el listado completo al tocarla. Tabla nueva `notificaciones` (migración `create_notificaciones_system`) **alimentada 100% por triggers de DB** (`SECURITY DEFINER`, mismo patrón que `get_trending_garments`) sobre `follows`/`outfit_likes`/`outfits_guardados`/`preguntas` — ninguna pantalla del cliente inserta una notificación a mano, así funciona sin importar el camino de escritura. RLS: cada uno solo puede leer/marcar como leídas las propias; sin policy de INSERT para usuarios normales. Realtime habilitado (mismo patrón que `outfits`) para que la campanita y la lista se actualicen solas. Nuevo hook `hooks/useNotifications.ts` (lista + `unreadCount` + `markAsRead`/`markAllAsRead`, con suscripción realtime) y pantalla `app/notifications.tsx` (5 tipos de fila con mensaje + destino de navegación propio, tocar marca como leída). Componente `NotificationBell` compartido entre `ConsumerHomeView` y `BrandHomeView` — rosa si `unreadCount > 0`, negra si no (mismo criterio de color-por-estado que `filtro_negro`/`filtro_rosa`, sin badge numérico). **Bug real encontrado y arreglado durante el testing** (no un bug preexistente, del código nuevo de esta sesión): el primer signup de una cuenta de prueba crasheó con `cannot add postgres_changes callbacks... after subscribe()` en `NotificationBell` — la campanita vive en el header de Home, montada/desmontada rápido durante la transición justo después del signup, y una segunda invocación del efecto de suscripción realtime pisó un canal ya suscripto con el mismo topic. Fix defensivo en `useNotifications.ts`: antes de crear el canal, busca uno existente con el mismo topic en `supabase.getChannels()` y lo remueve primero. **Verificado de punta a punta en browser real** generando notificaciones reales entre una cuenta de prueba descartable, `capas@opa.com` y `mateo.h` (usuario real, sin tocar su sesión): seguir a Capas → notificación real, tocarla marca como leída; preguntar sobre una prenda de Capas → notificación real para Capas, tocarla navega a `/brand/questions`; dar like + guardar un outfit de `mateo.h` → notificaciones reales para su cuenta (confirmadas en la DB); Capas responde la pregunta → notificación `question_answered` real para la cuenta de prueba, campanita cambia de color sola (realtime, sin recargar), tocarla navega a la prenda. **Todo el rastro de la prueba limpiado al terminar** — cuenta de prueba borrada, pregunta de prueba borrada (cascada limpia sus notificaciones), notificaciones residuales para Capas/mateo.h borradas a mano, contadores de Capas y del outfit de mateo.h confirmados de vuelta en su valor original. `tsc` limpio. Detalle completo en `database-2026-06-06-schema-and-seed.md` (tabla `notificaciones`), `backend-2026-06-06-supabase-integration.md` (`useNotifications.ts`, con el detalle completo del bug) y `frontend-2026-06-06-screens-and-components.md` ("Home", "Notifications").
- [x] **Emojis del buscador general reemplazados por assets reales de Storage (2026-09-14, sesión nueva):** `app/(tabs)/search.tsx` usaba 🔍 en la search bar y 👗 en el estado vacío inicial — reemplazados por `nav/search.png` (ya existía, usado en el navbar) y `camiseta.png`, ambos con `tintColor`, siguiendo el mismo protocolo de "chequear Storage antes de usar un emoji" ya documentado en Gotchas de proceso. De paso se agregó el ícono `nothing_found.png` (ya estaba subido a Storage) arriba del texto "Sin resultados para "{query}"" en los 3 tabs (Outfits/Prendas/Cuentas) — a pedido explícito del usuario, que además pidió que el texto muestre la búsqueda real entre comillas (ya lo hacía, solo le faltaba el ícono). Verificado en browser real: lupa e ícono de remera visibles y con el tint correcto; buscar algo inexistente muestra la figura de "sin resultados" + el texto con la query real interpolada.
- [x] **Moderación básica por longitud + contador de caracteres en Crear/Editar prenda (2026-09-14, misma sesión):** a pedido explícito del usuario ("agregues moderación básica a los textos estableciendo un límite en cada uno", después "que agregues un contador que muestre cuál es el límite... estilo 4/60"). `app/brand/create-garment.tsx`: `maxLength` en Nombre (60), Descripción (500), Precio (9 dígitos), URL externa en modo redirect (300), y el input "Otro" de los `DropdownField` de Color/Estilo (30) — es moderación por longitud únicamente, no valida contenido ni lenguaje. Contador "X/N" (`styles.charCounter`) debajo de cada campo de texto (no en Precio, es numérico) que incrementa en vivo con `.length` sobre el estado ya existente. Verificado en browser real logueado como Capas: nombre de 132 caracteres se corta en 60; escribir "Trench" en Nombre muestra "6/60" en tiempo real.
- [x] **Galería multi-imagen por prenda, hasta 5 (2026-09-14, misma sesión, pedido más grande):** antes cada prenda tenía como máximo 1 foto (`prendas.image_url`, `NOT NULL`) — decisión que en su momento se había documentado como "deliberadamente fuera de alcance" (ver `product-2026-06-10-brand-system.md`). Es un cambio de esquema + producto, así que antes de tocar nada se confirmaron 3 decisiones con el usuario (las 3 recomendadas): (1) tabla relacional nueva `prenda_imagenes` en vez de convertir `image_url` a array (permite reordenar/borrar sueltas a futuro sin otro cambio de esquema, y no rompe las +10 pantallas que ya leen `image_url` como string único); (2) alcance completo — no solo subir varias fotos en `create-garment.tsx`, sino también mostrar el carrusel completo en `product/[id].tsx`; (3) límite de 5 imágenes por prenda (de producto, no de DB). **DB:** tabla nueva `prenda_imagenes` (`garment_id` FK `ON DELETE CASCADE`, `image_url`, `sort_order`) con RLS (`public_read_...` + `brand_owner_insert/delete_own_...`, mismo patrón de ownership que `brand_owner_update_own_prendas`), backfill de 1 fila por cada una de las 28 prendas reales existentes (su `image_url` actual como portada única). `prendas.image_url` se mantiene sin cambios como "la portada" (siempre = `sort_order 0`) — así ningún otro lugar de la app (search, home, grillas, chips del outfit scroll) necesitó tocarse. **Frontend:** `create-garment.tsx` pasó de 1 imagen a una grilla de miniaturas (hasta 5, badge "Portada" en la primera, botón de sacar por foto, tile "+ Agregar foto", contador "X/5"); al guardar sube las fotos nuevas y hace un reemplazo total de `prenda_imagenes` (simple, alcanza para el límite de 5) — va directo a Supabase, no por la API Hono, mismo patrón ya usado para `descontinuada`/`preguntas` (`opa-backend` no clonado esta sesión). `product/[id].tsx` gana un carrusel (`FlatList` horizontal `pagingEnabled` + dots de paginación, hook nuevo `hooks/useGarmentImages.ts`) en vez de una sola imagen; el zoom abre la imagen que esté visible en ese momento, no siempre la portada. **Limitación de verificación honesta:** el entorno de preview headless de esta sesión no tiene diálogo de OS para simular la selección múltiple de archivos, así que la subida real de varias fotos no se pudo ejercitar de punta a punta — se verificó por code review + `tsc`, por confirmar que `expo-image-picker@17.0.11` soporta `allowsMultipleSelection`/`selectionLimit` (con `multiple` en el `<input type=file>` en su implementación web), por la precarga real en modo edición (portada real de "Trench Camel" con badge y botón de sacar), y por el carrusel de lectura completo insertando una segunda fila de prueba directo en la DB (swipe real confirmado, dots correctos por DOM, fila de prueba borrada después). Falta la confirmación de una subida real de 2+ fotos desde un dispositivo/browser normal. Detalle completo en `database-2026-06-06-schema-and-seed.md` (`prenda_imagenes`) y `frontend-2026-06-06-screens-and-components.md` ("Product Detail", "Create Garment").

## Pendientes principales
- [ ] **Crear outfit desde una cuenta de marca** — no existe ninguna pantalla para esto en `opa-mobile` (se investigó explícitamente, no hay ningún `create-outfit` ni equivalente). La Home de marca (bullet de arriba) linkea "Tus outfits publicados" al Catálogo existente en vez de a un flujo de creación real. Probablemente el gap más grande que queda en todo el sistema de marcas.
- [ ] **`preguntas` no pasa por la API Hono** — mismo patrón que `descontinuada` en `prendas`, va directo a Supabase por RLS. A diferencia de like/save, no tiene rate limiting; no es explotable de forma grave (`user_id = auth.uid()` siempre), pero valdría la pena mover si `opa-backend` está clonado en una sesión futura.
- [ ] **⚠️ "Eliminar cuenta" falla con 400 al confirmar con contraseña (encontrado 2026-09-07, no investigado a fondo):** al limpiar una cuenta de prueba después de verificar la feature de seguidores/seguidos, `Settings → Eliminar cuenta` con la contraseña correcta tiró "Ocurrió un error al eliminar la cuenta" dos veces seguidas (confirmado que el input sí tenía el valor correcto). La consola mostró un `400` de red sin más detalle capturado. No es un bug de esta sesión — el flujo no se tocó — pero tampoco se investigó la causa (¿reautenticación con `signInWithPassword` fallando por algo del entorno, o un bug real del RPC `delete_user()`?). Se terminó borrando la cuenta de prueba a mano vía SQL (`DELETE FROM auth.users`, cascada limpia confirmada). Antes de asumir que es solo un artefacto de este entorno, probarlo en un browser real / dispositivo.
- [ ] **Diferencias web ↔ prototipo del outfit scroll BLOQUEADAS por falta de datos/backend (2026-07-06):** (1) **Precisión de las líneas conectoras**: ya están implementadas pero el ancla es aproximada **por slot** (todas las prendas de un mismo slot apuntan al mismo punto). Para que cada prenda apunte a su lugar exacto haría falta agregar columnas `position_x/position_y` a `outfit_items` (+ un editor de posición); `constants/mockData.ts` ya tiene coords mock que nunca se volcaron a la DB. (2) **Badge de descuento "X% OFF / Ahorrás $Y"** en la barra de precio: no hay campo de descuento en `Outfit` ni `Garment`; el doc lo pide condicional a `discount_percent > 0` pero ese dato no existe. (3) **Pie con marca (logo + nombre + verificado) en vez de creador**: las `marcas` tienen `profile_id = null` (falta onboarding de cuentas de marca), así que el scroll muestra outfits de usuarios; el footer seguirá mostrando el creador hasta que exista ese onboarding.
- [ ] Arreglar orden de middlewares en `opa-backend/functions/api/index.ts`: el rate-limiter de `POST /orders` corre antes que `authMiddleware`, así que `c.get('user')` está vacío y el límite nunca se aplica (bug preexistente, no bloqueante)
- [ ] Settings sub-screens: editar perfil, seguridad, notificaciones, preferencias de estilo
- [x] **Revisión y actualización de `.claude/documents/` (2026-08-03):** los 5 docs de capa que habían quedado atrás de varias sesiones de código (varios fechados 2026-06-06/09 sin tocar desde entonces) se pusieron al día contra el código real: `backend-2026-06-15-api-layer.md` y `backend-2026-06-06-supabase-integration.md` (referencia de repo `maxibernardoni/opa-backend` → `opa-organization/opa-backend`, párrafo contradictorio sobre `delete_user()` no creada — sí existe, tipos TS desactualizados, hooks `useBrand`/`useMyBrand` faltantes), `database-2026-06-06-schema-and-seed.md` (faltaban 2 migraciones en la tabla, 4to perfil seed de la cuenta de marca Revés), `frontend-2026-06-06-screens-and-components.md` (no mencionaba `app/marca/[id].tsx`, el login de marca, `MobileFrame.tsx`/`constants/layout.ts`, ni ninguno de los ajustes de fidelidad al prototipo del outfit scroll de julio) y `design-2026-06-06-visual-system.md` (líneas conectoras seguían documentadas como `position_x/position_y` cuando la implementación real usa anclas por `slot`, tamaños de botones de acción viejos, inventario de assets incompleto). También se recreó `.env` (URL + anon key vía Supabase MCP `get_publishable_keys`) porque no viaja entre sesiones. **No se tocó código**, solo documentación — si algo de esto se contradice con lo que ves en el código, el código manda (ver Gotchas de proceso).
- [ ] Replicar este formato de `CLAUDE.md` (secciones "Cómo trabajar conmigo", mapa de repos, valores, glosario, snapshot en prosa) en `opa-admin` y a futuro en `opa-backend` / `opa-web`
- [x] **`opa-admin` confirmado como panel funcional, no "sin iniciar" (2026-08-03):** se clonó el repo (`C:\Users\devandroid\opa-admin`) y se verificó directamente (file tree, `git log`, `git remote -v`) que ya cubre login, auth gate por `is_admin`, dashboard, gestión de usuarios y marcas, y moderación de outfits/prendas/reseñas — corriendo local contra Supabase real, sin deploy. Se corrigieron `meta-2026-06-10-pending-features.md`, `product-2026-06-15-admin-panel.md` y este archivo, que decían "no iniciado"/"en desarrollo inicial". El único gap real de Brand Management es la pantalla de solicitudes (`brand_applications` sin UI). De paso se corrigió el mismo error en `opa-admin/CLAUDE.md` (`maxibernardoni/opa-backend` → `opa-organization/opa-backend`, estado de deploy independiente).
- [x] **DB: `size_guide_id` asignado a las 25 prendas seed + columna `foot_length` en `user_measurements` (2026-08-03):** ver detalle en `meta-2026-06-10-pending-features.md` → Database → General. Quedaron sin resolver, a la espera de una decisión del usuario: restaurar `size`/`color`/`source` en `prendas_armario` (sin consumidor todavía, no hay flujo de compra) y agregar `position_x`/`position_y` a `outfit_items` (requiere además ubicar cada prenda a mano sobre la foto de cada uno de los 7 outfits reales, no es solo la migración).
- [ ] **⚠️ Migración/tabla `admin_impersonation_log` sin documentar, encontrada 2026-08-03** — aplicada en producción el mismo día (versión `20260803115219`), antes del trabajo de esta sesión. Tabla de auditoría (`admin_profile_id`, `brand_id`, `brand_profile_id`, `created_at`), RLS sin policies (solo `service_role`). Pinta a una feature de "un admin impersona una cuenta de marca" hecha en alguna sesión de `opa-admin` no sincronizada a ningún doc. No se tocó. **Antes de asumir qué es, confirmalo** — ver nota completa en `database-2026-06-06-schema-and-seed.md`.
- [x] **Paginación cursor-based en `useOutfits` (2026-08-10):** reemplaza el `LIMIT 20` fijo por un cursor keyset sobre `(created_at, id)` — `id` hace falta como desempate porque varios outfits seed comparten el mismo `created_at` exacto (mismo INSERT), y un cursor solo con `created_at` saltea/duplica filas en ese borde (verificado con SQL directo antes de tocar el hook). Nueva forma del hook: `{ outfits, loading, loadingMore, hasMore, error, loadMore, refetch }`. Conectado a `onEndReached` en los dos scrolls verticales tipo TikTok (`app/(tabs)/outfits.tsx`, `app/user-outfits.tsx`); las grillas más chicas (perfil, `user/[id].tsx`, carousel de home) siguen mostrando solo la primera página — no era el caso de uso que motivaba el pendiente. Verificado en browser bajando `PAGE_SIZE` a 3 temporalmente: 3 páginas trajeron los 7 outfits seed sin duplicados ni saltos, después se repuso a 10.
- [x] **Realtime de contadores like/save (2026-08-10):** `hooks/useLike.ts`/`useSave.ts` se suscriben a `postgres_changes` (UPDATE) sobre la fila de `outfits` de cada outfit, y toman `likes_count`/`saves_count` directo de la fila confirmada en cada evento — no suman/restan a mano, así no hay drift, y el contador se actualiza también cuando el cambio lo hace **otro** usuario, no solo el propio. Hubo que agregar `outfits` a la publicación `supabase_realtime` (migración `20260810120500_enable_realtime_on_outfits`) — ninguna tabla del proyecto tenía Realtime habilitado hasta ahora. Dos canales por card de outfit (uno por hook) en vez de uno compartido — el cambio más simple dado el límite de cada hook, no ameritaba reestructurar `OutfitScrollItem`. **Verificado con dos pestañas de browser separadas** (una viendo el feed sin login, otra logueada con una cuenta descartable dando like/save): el contador de la pestaña que nunca tocó nada se actualizó solo, sin refetch — confirmado con un `console.log` temporal (la UI no muestra el número en pantalla, es a propósito, ver `design-2026-06-06-visual-system.md`), sacado después de verificar. Cuenta de prueba borrada al terminar.
- [x] **Edge Functions server-side para like/save + rate limiting real (2026-08-10):** el más grande de los 4 pendientes de backend de esta sesión. Nuevo `opa-backend/functions/api/routes/social.ts` — `POST/DELETE /api/outfits/:id/like` y `.../save`, idempotentes, con rate limiting (60 req/60s por usuario) y bloqueo server-side de cuentas de marca (RLS solo chequeaba `user_id = auth.uid()`, no `is_brand` — el bloqueo de like/save de marca de antes era puramente cosmético en el cliente). `opa-mobile` gana `lib/api.ts` — **primera vez que esta app llama a la API Hono**; todo lo demás (incluido el chequeo de estado inicial liked/saved) sigue yendo directo a Supabase. `hooks/useLike.ts`/`useSave.ts` actualizados para llamar la API en el toggle. **Dos bugs de plataforma reales encontrados y arreglados en el camino** (no bugs de mi código nuevo — del rate limiter preexistente, que nunca había funcionado de verdad pese a estar "implementado"): (1) el rate limiter corría antes que `authMiddleware`, bug ya documentado pero nunca arreglado — se resolvió montándolo por grupo de rutas, después del auth de ese grupo; (2) incluso arreglado el orden, seguía sin funcionar — confirmé empíricamente que Supabase Edge Functions **no mantienen estado de módulo entre invocaciones** (un `Map` a nivel de módulo era una instancia nueva en cada request individual, incluso en ráfagas secuenciales rápidas). El plan original era migrar a Deno KV, pero `Deno.openKv()` **no existe** en este runtime (`TypeError: Deno.openKv is not a function`) — terminé armando el rate limiter sobre Postgres (tabla `rate_limits` + función `increment_rate_limit()` con `INSERT ... ON CONFLICT` atómico). Todo verificado con requests HTTP reales (cuentas de prueba descartables vía signup, borradas después): like/unlike/save/unsave idempotentes, bloqueo 403 a cuenta de marca real (`capas@opa.com`), 404 a outfit inexistente, rate limit disparando en la request #4 con `max` bajado a 3 temporalmente para probar, y el flujo completo desde el browser real (signup descartable → like/save en el feed → verificado en la DB → unlike/unsave → cuenta borrada). CORS del API actualizado para incluir `localhost:8090` (antes solo contemplaba `opa-web`, nunca `opa-mobile`). Detalle completo en `backend-2026-06-15-api-layer.md` (secciones Social y Rate Limit Middleware) y `backend-2026-06-06-supabase-integration.md`.
- [x] **Full-text search en outfits y prendas (2026-08-10):** reemplaza el `.ilike()` de `app/(tabs)/search.tsx`, que solo matcheaba `title`/`name` — no encontraba nada buscando por texto de la descripción ni por nombre de marca. Migración `20260810110614_add_full_text_search_outfits_prendas` (Supabase real + `opa-backend/supabase/migrations/`): columna `search_vector tsvector` (config `'spanish'`) + índice GIN en `outfits` (`title`+`description`, columna `GENERATED`) y en `prendas` (`name`+`description`+nombre de marca, mantenida por dos triggers porque el nombre de marca es de otra tabla — uno recalcula al crear/editar la prenda, otro recalcula las prendas de una marca si se le cambia el `name`). Query nueva: `.textSearch('search_vector', query, { type: 'websearch', config: 'spanish' })`. Verificado en browser: buscar "Capas" en Prendas trae sus 4 prendas (ninguna se llama literalmente "Capas"); buscar "vestido" en Outfits trae dos outfits por stemming en español ("un vestido..." y "vestirse bien", misma raíz léxica). De paso se commiteó en `opa-backend` una migración de una sesión anterior (`add_size_color_source_to_prendas_armario`) que había quedado aplicada en Supabase pero sin subir a git.
- [x] **Cuentas de marca sin acceso al feed + switcher multi-cuenta (2026-08-07):** dos pendientes del brand-system resueltos en la misma sesión. (1) **Bloqueo de marca:** a pedido explícito del usuario, el alcance se amplió de "ocultar botones de like/save/follow" a **bloquear el acceso a la sección de feed entera** para cuentas de marca — `app/(tabs)/outfits.tsx` redirige (`<Redirect>`) a `/(tabs)/profile` si `profile.is_brand`, cubriendo tab press + deep-link desde Home + URL directa en un solo lugar; el ícono "outfits" desaparece de `BottomNavBar` y de las navbars standalone de `marca/[id].tsx`/`user/[id].tsx`; el botón Seguir se oculta para viewers de marca en esos mismos dos perfiles; y `OutfitScrollItem` oculta like/save/follow (mantiene compartir) como defensa adicional, porque igual es alcanzable fuera del tab feed vía `user-outfits.tsx`/`saved-outfits.tsx`. (2) **Switcher multi-cuenta:** como no existe ningún vínculo en la DB entre una cuenta personal y una de marca (son usuarios de Auth totalmente separados), se implementó como "cuentas recordadas en este dispositivo" en vez de un switcher estilo Instagram real — decisión confirmada con el usuario antes de programar. Nuevo `lib/rememberedAccounts.ts` guarda `access_token`/`refresh_token` por cuenta (mismo storage que `lib/supabase.ts`, ahora exportado); `app/_layout.tsx` los actualiza en cada `SIGNED_IN`/`TOKEN_REFRESHED` (Supabase rota el refresh_token, así que hay que mantenerlo al día) y los borra en `SIGNED_OUT` (cerrar sesión = "salir de este dispositivo", coherente con el texto ya existente en Settings). Nueva pantalla `app/switch-account.tsx` (accesible desde Settings → "Cambiar de cuenta", ícono `⇄` en texto porque no hay asset para esto en Storage) lista las cuentas guardadas y cambia entre ellas con `supabase.auth.setSession()` — sin contraseña. Todo verificado en el browser real: login Capas → agregar Sole → cambiar Sole↔Capas sin re-auth → logout de Capas la saca del switcher mientras Sole sigue funcionando. `tsc` sin errores nuevos.
- [x] **Tab "tus marcas" del outfit scroll filtra de verdad (2026-08-07):** antes era solo visual (`setTab` sin efecto en la data). Ahora `app/(tabs)/outfits.tsx` filtra el feed a outfits con al menos 1 prenda de una marca que el usuario sigue (criterio elegido por el usuario entre 3 opciones: "al menos 1 prenda" vs. "mayoría" vs. "exclusivo" — se optó por el más laxo porque con solo 4 marcas seguibles hoy, un criterio más estricto hubiera dejado el tab casi siempre vacío). Hallazgo importante: el pendiente decía que esto "requiere extender el sistema de follows a marcas", pero **ya funcionaba** — `marca/[id].tsx` ya sigue marcas a través de la misma tabla `follows` (una marca tiene `perfiles.id` = `marcas.profile_id`, así que `following_id` apunta ahí sin cambios de schema). Nuevo hook `hooks/useFollowedBrandIds.ts`. Estados vacíos agregados para 0 marcas seguidas / 0 outfits con match. Verificado en browser real: login `capas@opa.com`, seguir a Sole (en 7/7 outfits seed) → aparecen 7; dejar de seguir → estado vacío; "Descubrir" sin regresión. Se dejó la sesión de browser sin login al terminar. **Bug de UI encontrado y arreglado (2026-08-07):** el contador "Seguidores" en `marca/[id].tsx` no se actualizaba tras tocar Seguir/Siguiendo. Fix: `useBrand` ahora expone `adjustFollowersCount(delta)` (ajuste optimista, mismo patrón que `useLike`/`useSave`); `marca/[id].tsx` lo llama antes de disparar el toggle real. Verificado en browser (login `sole@opa.com`, follow/unfollow de Capas, contador se actualiza al instante sin remount, sin residuos en `follows`).
- [ ] **Decisión de producto: `position_x`/`position_y` de `outfit_items` no se completan a mano (2026-08-07):** el usuario descartó que hand-placeemos las coordenadas de las 7 prendas/outfits seed. En cambio, la marca va a ubicar cada prenda de forma visual (paso tipo "ubicá esta prenda sobre la foto") al crear/editar una prenda — esa UI todavía no existe, probablemente va a vivir en `opa-web` (repo sin iniciar). Hasta que eso se construya, seguimos sin tocar el schema ni el render de líneas conectoras (`OutfitScrollItem` sigue usando anclas por `slot`). Detalle completo en `meta-2026-06-10-pending-features.md` y `design-2026-06-06-visual-system.md`.
- [x] **DB: columnas `size`/`color`/`source` restauradas en `prendas_armario` (2026-08-07):** el usuario decidió agregarlas ahora aunque todavía no existe flujo de compra que las consuma (antes estaba marcado como pendiente de decisión). Migración `20260807141330_add_size_color_source_to_prendas_armario` aplicada en Supabase real y commiteada en `opa-backend/supabase/migrations/`; las 3 son `varchar` nullable sin constraint. La tabla tenía 0 filas (nadie carga su armario todavía) así que no hubo datos que migrar/poblar. `types/index.ts` (`WardrobeItem`) actualizado para reflejar los campos nuevos. Sin UI ni hook (`useWardrobe`) actualizados todavía — no hay pantalla que edite `size`/`color`/`source` de un ítem del armario.
- [ ] **La tabla de migraciones de `database-2026-06-06-schema-and-seed.md` tenía versiones inventadas** (no las reales aplicadas en la DB) desde que existe el doc — se corrigió comparando contra `list_migrations` real (2026-08-03). Al menos un caso confirmado de drift real (no solo del doc): el archivo local `opa-backend/supabase/migrations/20260629000001_rename_marcas_owner_id_to_profile_id.sql` tiene un prefijo de fecha distinto al que quedó aplicado en la DB (`20260629142025`). No investigado más a fondo — si se necesita reparar el historial de migraciones (`supabase migration repair`) o diffear local vs. remoto, tenerlo en cuenta.
- [x] **Buscador general mejorado para compradores y marcas navegando OPA (2026-08-14):** `app/(tabs)/search.tsx` — a pedido del usuario, que confirmó vía preguntas que el alcance era "el buscador general" (no el catálogo propio de una marca en `wardrobe.tsx`, que se dejó afuera a propósito). Cuatro agregados, todos sin tocar la DB (columnas que ya existían — `category`, `price`, `marcas.name/description` — cero dependencia de `opa-backend` o Supabase MCP, no autorizado esta sesión): (1) **tab "Marcas"** nueva — buscar una marca por nombre/descripción y navegar directo a `marca/[id]`, algo que antes no existía (solo se podía llegar a una marca navegando desde otro lado); (2) **chips de categoría real** en Prendas (Torso/Piernas/Calzado/Extras, el enum real de `prendas.category`) reemplazando `style` a mano; (3) **chips de Outfits ahora dinámicos** — se traen los valores reales de `style`/`occasion` que existen hoy en la tabla en vez de una lista fija de 13 tags inventados que no coincidía con el seed y devolvía 0 resultados en varios; (4) **filtro de precio** (min/max, solo Prendas) y **selector de orden** (Outfits: Populares/Recientes; Prendas: Recientes/Precio ↑/Precio ↓; Marcas: sin selector, alfabético fijo). Verificado en browser real: tab Marcas con "Capas" trae la marca correcta; combinar precio mínimo + orden ascendente + chip de categoría en Prendas filtra y ordena bien en simultáneo. `tsc` sin errores nuevos (los de `constants/mockData.ts` son preexistentes, archivo sin uso real). Detalle completo en `frontend-2026-06-06-screens-and-components.md`.
- [ ] **⚠️ `opa-backend` desactualizado respecto al Edge Function `api` en producción (desde 2026-08-14):** el fix de CORS (ver bullet de arriba, versión 12 del function) se deployeó directo vía Supabase MCP sin tocar el repo `opa-backend` (no estaba clonado esta sesión). Cuando una sesión tenga ese repo clonado: bajar el código real (`get_edge_function` del MCP, o `supabase functions download api`) y commitear el `index.ts` actualizado (el bloque de `cors` ahora usa un regex `localhost:*` en vez de una lista fija de puertos).
- [ ] **Angostar la policy RLS `brand_owner_update_own_prendas` (agregada 2026-08-14):** hoy deja a una marca actualizar *cualquier* columna de sus propias `prendas` directo por Supabase (se agregó así porque el toggle de `descontinuada` necesitaba escribir algo que el PATCH de la API todavía no acepta — ver bullet de arriba). Cuando una sesión tenga `opa-backend` clonado: agregar `descontinuada` a la whitelist de `PATCH /api/brands/me/prendas/:id`, y evaluar si la policy se puede eliminar (todo pasaría por la API de nuevo) o angostar a esa sola columna (ej. con un trigger).
- [x] **Pulido del buscador general a pedido del usuario, con screenshot de referencia (2026-09-04):** tres ajustes sobre `app/(tabs)/search.tsx`, sesión aparte de la que lo creó (2026-08-14). (1) **Bug visual real, no reportado antes:** el usuario mandó un screenshot mostrando el chip de categoría activo (tab Prendas, fondo negro) como una barra sólida sin texto legible. Investigado a fondo con `getBoundingClientRect`/CSS computado en vez de asumir — es una **segunda variante, opuesta**, del bug de `FlatList` horizontal ya documentado (ver CLAUDE.md → Decisiones técnicas críticas): con `flexGrow:0` ya puesto, la fila de tags al ser hermana del `FlatList` de resultados dentro de un `flex:1` se **encoge** (default `flex-shrink:1`) para cederle espacio al grid apenas el contenido no entra en pantalla, hasta el mínimo de una línea de texto (~16px) — invisible con fondo blanco, evidente con el chip activo de fondo negro. Un primer intento con solo `height:44` no alcanzó; el fix real fue sumar `flexShrink: 0`. (2) **Sin `#` en los chips de categoría de Prendas** ("Calzado", no "#Calzado") — a pedido explícito, los de Outfits (`#street`, etc.) se dejaron igual porque son hashtags de estilo, no un enum. (3) **Tab "Marcas" → "Cuentas":** ahora busca en paralelo `marcas` y `perfiles` (excluyendo perfiles `is_brand=true`, ya cubiertos por `marcas`), mostrando cada resultado con una etiqueta "MARCA"/"USUARIO" junto al nombre y navegando a `/marca/[id]` o `/user/[id]` según corresponda — decisiones de label de tab ("Cuentas" vs. otras opciones) y de cómo distinguir marca/usuario en la lista (etiqueta chica vs. secciones separadas vs. solo forma del avatar) confirmadas con el usuario antes de programar. Verificado extensamente en browser real: chip activo legible, búsqueda "sole"/"capas" trae la marca correcta, búsqueda "a" trae marcas y usuarios mezclados con su etiqueta, tap en un usuario navega a su perfil. `tsc` sin errores nuevos. Detalle completo en `frontend-2026-06-06-screens-and-components.md`.
